# FR-08: Экспертная труба

Раздел, в котором студент находит эксперта с валидным опытом и записывается к нему на консультацию. Запрос проходит двойную проверку — модератор, затем эксперт, — после чего стороны обмениваются контактами. Через 7 дней система собирает обратную связь с обеих сторон.

**Статус документа:** согласован по архитектуре и доступам. Состав полей карточки эксперта и анкеты запроса — черновик, помечен «требует подтверждения» (см. раздел 11).

---

## 1. Требования

| ID | Требование | Описание / Критерии приёмки | Приоритет |
|----|------------|------------------------------|-----------|
| 08.01 | Экран выбора пространства | При заходе на сайт авторизованный пользователь видит две плитки: «Платформа ВКР» и «Экспертная труба». Сессия не сбрасывается, пароль не запрашивается. | Must |
| 08.02 | Три состояния плитки | Открыто (кликабельна) / Закрыто замком (некликабельна) / Приглашение (ведёт на подключение роли эксперта). | Must |
| 08.03 | Переключатель пространств | Переключение между пространствами доступно из сайдбара в любой момент, без возврата на экран выбора и без повторного входа. | Must |
| 08.04 | Диплинки минуют экран выбора | Переход по ссылке из письма или уведомления открывает целевую страницу напрямую. | Must |
| 08.05 | Самоподключение роли эксперта | НР подключает себе роль эксперта сам, одним нажатием, без модерации и без повторного входа. | Must |
| 08.06 | Приглашение внешних экспертов | Админ создаёт аккаунты внешних экспертов из админки трубы. Доступа к «Платформе ВКР» у них нет. | Must |
| 08.07 | Доступ студентов по когортам | Админ отмечает, каким потокам труба открыта. Проверка на сервере, не только в интерфейсе. | Must |
| 08.08 | Карточка эксперта | Эксперт заполняет карточку сам. Модерация карточки не требуется — карточка попадает в каталог сразу после заполнения обязательных полей. Резюме обязательно. | Must |
| 08.09 | Скрытие карточки | Эксперт может скрыть карточку из каталога (отпуск, перегрузка) и вернуть обратно. | Must |
| 08.10 | Каталог для студента | Список карточек, открытие карточки, фильтр по тегам доменной экспертизы. | Must |
| 08.11 | Контакты под замком | Контакты эксперта видны только студенту с принятым запросом. В каталоге и в карточке до принятия их нет. | Must |
| 08.12 | Анкета запроса | Пять полей: что обсудить (от 500 символов), какой результат жду (от 80), что уже сделал сам (от 500), где возникли проблемы и ссылка на папку с материалами — необязательные. Программа и курс из профиля, проект привязывается по желанию. Файлы не прикладываются. | Must |
| 08.13 | Очередь модератора | Админ видит новые запросы, одобряет или отклоняет. При отклонении причина обязательна и видна студенту. | Must |
| 08.14 | Решение эксперта | Эксперт видит анкету, имя и программу студента (без контактов), принимает или отклоняет. | Must |
| 08.15 | Обмен контактами | После принятия студент получает контакты эксперта, эксперт — контакты студента. Каналы: email + уведомление в платформе. | Must |
| 08.16 | Сбор обратной связи | Через 7 дней после отправки контактов система запрашивает обратную связь у студента и у эксперта. Форма встроена в карточку запроса, отдельных страниц нет. Студент отвечает про встречу, оценку 1–5 и комментарий, эксперт — про встречу и комментарий. Ответ студента закрывает запрос, ответ эксперта — нет. | Must |
| 08.17 | Напоминание об обратной связи | Если студент не ответил за 3 дня, система отправляет одно напоминание. | Should |
| 08.18 | Раздельность заявок | Экспертные запросы и заявки на научное руководство хранятся в разных сущностях и не смешиваются ни в одном списке. | Must |
| 08.19 | Метрика конверсии | Админ видит конверсию «запрос → состоявшаяся встреча» на основе двух ответов об одной и той же встрече. Запросы без обратной связи — отдельная категория «нет данных». | Should |
| 08.20 | Авто-закрытие без обратной связи | Если студент не ответил через 7 дней после запроса обратной связи, система закрывает запрос с пометкой «без ответа». | Should |
| 08.21 | Отсутствие лимитов на запросы | Система не ограничивает количество запросов ни у студента, ни у эксперта. Единственная защита — запрет дубля в необработанном статусе. | Must |
| 08.22 | Уведомления по пространствам | Счётчик и список уведомлений показывают только текущее пространство. На переключателе — бейдж «есть непрочитанные в другом». | Must |

---

## 2. Роли и доступы

| Роль | Как получает доступ к трубе | Что делает |
|------|------------------------------|------------|
| Админ (`ADMIN`) | Всегда, без условий | Модерирует запросы, приглашает внешних экспертов, открывает когорты, смотрит метрики |
| Научный руководитель (`SUPERVISOR`) | Сам нажимает «Принять участие в роли эксперта» | Размещает карточку, принимает и отклоняет запросы |
| Внешний эксперт (`EXPERT`) | Аккаунт создаёт админ из админки трубы | То же, что НР-эксперт. Доступа к «Платформе ВКР» нет |
| Студент (`STUDENT`) | Когорта студента отмечена админом как открытая | Смотрит каталог, отправляет анкету, оставляет обратную связь |

**Модератор — это существующая роль `ADMIN`.** Новой системной роли модератора не появляется.

**Роль эксперта не хранится в поле `User.role`.** Поле одно и у научного руководителя занято значением `SUPERVISOR` — перезапись отрежет его от научного руководства. Признак роли эксперта — наличие записи `ExpertProfile` у пользователя. Значение `EXPERT` в перечислении `Role` нужно только для внешних экспертов, у которых нет другой роли на платформе.

Выхода из роли эксперта нет. Если эксперт больше не хочет получать запросы, он скрывает карточку (08.09).

---

## 3. Навигация и пространства

### 3.1 Экран выбора

Новая страница `/spaces` — две плитки. Авторизованный пользователь, открывший корень сайта, попадает на неё; неавторизованный видит текущий лендинг. Редирект после логина ведёт туда же вместо текущей развилки по ролям.

Последний выбор не запоминается — плитки показываются при каждом заходе на сайт.

Название раздела на плитке и в интерфейсе — «Экспертная труба», одинаково для всех ролей, включая студентов.

Переход по прямой ссылке (письмо, in-app уведомление, закладка) экран выбора не показывает и ведёт на целевую страницу.

### 3.2 Состояния плиток

| Кто смотрит | «Платформа ВКР» | «Экспертная труба» |
|-------------|------------------|---------------------|
| Админ | Открыто | Открыто |
| НР с ролью эксперта | Открыто | Открыто |
| НР без роли эксперта | Открыто | **Приглашение** — «Принять участие в роли эксперта» |
| Студент | Открыто, если поток открыт для платформы; иначе **замок** «Раздел ещё не открыт для вашего потока» (A1) | Открыто, если поток открыт для трубы; иначе **замок** |
| Внешний эксперт | **Замок**, некликабельна | Открыто |

Плитка с замком не кликается — ни тултипа, ни модалки. Прямой переход по URL закрытого пространства отдаёт заглушку, а не 404.

### 3.3 URL

Труба живёт под префиксом `/expert/*` — отдельная route-группа со своим лейаутом и сайдбаром. Контекст пространства определяется URL, а не cookie: иначе ломаются диплинки из писем, открытие в новой вкладке и кнопка «назад».

**«Платформа ВКР» остаётся на текущих URL без префикса.** Полная симметрия, о которой договорились, — это симметрия интерфейса и админки, но не адресов: перевод платформы на `/vkr/*` сломал бы все существующие ссылки в отправленных письмах, уведомлениях и закладках пользователей.

| URL | Кому | Назначение |
|-----|------|------------|
| `/spaces` | всем авторизованным | Экран выбора пространства |
| `/expert` | студент / эксперт | Точка входа: каталог для студента, кабинет для эксперта |
| `/expert/catalog` | студент | Каталог карточек, фильтр по темам |
| `/expert/catalog/[id]` | студент | Карточка эксперта, кнопка «Оставить запрос» |
| `/expert/requests/new?expertId=` | студент | Анкета запроса |
| `/expert/my-requests` | студент | Мои запросы со статусами, причинами отказа и встроенной формой обратной связи |
| `/expert/inbox` | эксперт | Входящие запросы, принять / отклонить, встроенная форма обратной связи |
| `/expert/profile` | эксперт | Карточка эксперта, скрытие карточки |
| `/expert/join` | НР без роли эксперта | Подключение роли |
| `/expert/admin/requests` | админ | Очередь модерации |
| `/expert/admin/access` | админ | Открытые когорты |
| `/expert/admin/invitations` | админ | Приглашение внешних экспертов |
| `/expert/notifications` | студент / эксперт / админ | Уведомления экспертной трубы (08.22) |
| `/expert/admin/metrics` | админ | Конверсия запрос → встреча |

### 3.4 Подключение роли без повторного входа

Механика уже обкатана в проекте на сохранении профиля (`app/profile/page.tsx` → `updateSession()` → ветка `trigger === "update"` в `lib/auth.ts`):

1. НР нажимает «Принять участие» на `/expert/join`.
2. `POST /api/expert/join` создаёт `ExpertProfile`, предзаполняя из `SupervisorProfile` фото, место работы, должность, звание, степень, резюме, экспертизу, направления и типы проектов. Дальше карточка правится отдельно от профиля НР.
3. Клиент вызывает `updateSession()`.
4. Ветка `trigger === "update"` в `jwt`-колбэке перечитывает из БД признак роли эксперта — сейчас она перечитывает только `profileCompleted` и `name`, её надо расширить.
5. Плитка разблокирована, пользователь внутри трубы.

Роль пускает в пространство сразу, но **в каталог карточка попадает только после заполнения обязательных полей**. До этого эксперт видит у себя баннер «заполните карточку, чтобы вас нашли».

---

## 4. Доступ студентов по когортам

Когорты — существующий справочник `cohorts` («Потоки»), который админ правит в `/admin/dictionaries`. Когорта присваивается студенту при создании аккаунта и уже обязательна.

Отдельной «даты открытия раздела» нет: галочка у когорты и есть механизм открытия, вторая правда в этом вопросе не нужна.

**Правила:**

- Труба открыта студенту, если значение его `StudentProfile.cohort` отмечено как открытое.
- Пустая когорта = доступа нет. В админке трубы выводится счётчик «студентов без потока: N», чтобы такие студенты не потерялись молча.
- Проверка когорты обязательна и на страницах `/expert/*`, и во всех API-роутах трубы. Замок на плитке закрывает только визуальный путь.
- Переименование значения справочника, у которого есть студенты, ломает белый список. Решение: при переименовании значения переносить доступ на новое значение либо запрещать переименование когорт с привязанными студентами.

### 4.1 Доступ к «Платформе ВКР» по потокам (A1)

Источник: [08-FR-08-requirements-v2.md](08-FR-08-requirements-v2.md), требование A1. Первокурсники получают трубу раньше платформы, и без отдельной настройки они видели бы научников и проекты второго курса.

- У каждого потока две независимые галочки в «Доступ по потокам»: «Экспертная труба» и «Платформа ВКР». Галочка одного пространства не меняет другое.
- Закрыто по умолчанию: поток без записи, пустой поток и поток вне справочника не видят ни трубу, ни платформу, пока админ их не откроет.
- Студенту закрытого потока закрыты все разделы платформы: проекты, научные руководители, заявки, мои проекты, календарь, база знаний и НИР. Прямая ссылка ведёт на заглушку «Раздел ещё не открыт», как в трубе.
- Уведомления платформы сужаются, а не закрываются: страница `/notifications` и её API отдают студенту закрытого потока только ответы поддержки (`SYSTEM` со ссылкой на `/inquiries`), потому что обращения ему открыты. 403 здесь не подходит — сайдбар и переключатель запрашивают счётчики на каждой странице.
- Навигация не ведёт в заглушки: в сайдбаре платформы у закрытого потока только «Профиль», «Уведомления» и «Обращения», чек-лист онбординга не показывается (`/api/onboarding` отдаёт пустые шаги).
- Открытыми остаются профиль (из него анкета запроса берёт программу и курс), обращения в поддержку, экспертная труба и экран выбора. После первого заполнения профиля студент попадает на `/spaces`, а не на `/my-projects`.
- Страницы закрывает `components/layout/VkrGate.tsx` в `layout.tsx` каждого раздела. Данные закрыты в API: страницы платформы клиентские, поэтому каждый обработчик платформенного API, доступный студенту, начинается с `denyVkrClosed()` из `lib/expert-access.ts` и отвечает 403 студенту закрытого потока и внешнему эксперту — это зеркало `canEnterVkrSpace` для API: middleware к `/api` не применяется, и до этой правки внешний эксперт читал базу знаний, календарь и проекты через API (08.02). Для админа, научника и анонимных вызовов проверка ничего не делает.
- База знаний закрыта по умолчанию (`lib/knowledge.ts`): всё видит только админ, студент и научник — по своим флажкам, остальные роли — ничего.
- Изменение галочки действует сразу, без повторного входа: поток проверяется при каждом запросе, в JWT он не кэшируется.
- Поток сравнивается без пробелов по краям везде: при записи флагов, при проверке студента и в панели «Доступ по потокам».
- **Правило для разделов платформы:** layout в Next — не граница доступа, сегмент страницы рендерится и уходит в RSC-ответ даже под заглушкой. Поэтому страницы в разделах платформы только клиентские, данные только через API с `denyVkrClosed()`. Серверная страница с запросом в БД обязана сама вызвать `canEnterVkrSpace()`.
- Анонимные публичные ручки каталога (`/api/supervisors`, `/api/supervisors/[id]`, список `/api/projects`, 01.08) остаются публичными: A1 закрывает платформу студенту в его аккаунте, а не публичный каталог.

**Выкатка.** Миграция `20260925120000_cohort_platform_access` добавляет флаг и сохраняет статус-кво: потокам, в которых на момент выкатки есть студенты, платформа открывается, иначе они потеряли бы доступ в момент миграции. **Сразу после выкатки** админ снимает галочку «Платформа ВКР» у Потока 2026 в «Доступ по потокам».

---

## 5. Модель данных

Черновик Prisma. Имена полей анкеты и карточки уточняются (раздел 11).

### 5.1 Правки существующих сущностей

```prisma
enum Role {
  ADMIN
  SUPERVISOR
  STUDENT
  EXPERT              // новое: внешний эксперт, без доступа к «Платформе ВКР»
}

enum NotificationType {
  // ... существующие значения
  EXPERT_REQUEST_NEW        // новый запрос — модератору
  EXPERT_REQUEST_APPROVED   // одобрен модератором — эксперту
  EXPERT_REQUEST_REJECTED   // отклонён модератором или экспертом — студенту
  EXPERT_REQUEST_ACCEPTED   // принят, контакты — студенту и эксперту
  EXPERT_FEEDBACK_REQUEST   // прошло 7 дней — обеим сторонам
}

model User {
  // ... существующие поля
  expert  ExpertProfile?
}
```

### 5.2 Карточка эксперта

Набор полей повторяет профиль научного руководителя (`SupervisorProfile`), с тремя отличиями: «Предлагаемые темы» заменены на «С какими темами могу помочь», поля про руководство проектами не переносятся, модерации карточки нет.

```prisma
model ExpertProfile {
  id              String   @id @default(cuid())
  userId          String   @unique
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  workplace       String   @default("")   // Место работы (она же компания)
  position        String   @default("")   // Должность
  academicTitle   String   @default("")   // Учёное звание — у внешних экспертов может не быть
  academicDegree  String   @default("")   // Учёная степень — у внешних экспертов может не быть
  resumeUrl       String?                 // Ссылка на резюме или файл
  photoUrl        String?                 // Фото
  expertise       String[]                // Доменная экспертиза (теги) — по ней фильтр каталога
  helpTopics      String?                 // «С какими темами могу помочь» — опционально,
                                          // на месте proposedTopics у НР
  directions      String[]                // Направления студентов
  projectTypes    String[]                // Типы проектов
  contact         String   @default("")   // Контакт — виден только студенту с принятым запросом

  cardCompleted   Boolean  @default(false) // обязательные поля заполнены → в каталоге
  hiddenByOwner   Boolean  @default(false) // скрыл сам (отпуск)

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  requests        ExpertRequest[]

  @@index([cardCompleted, hiddenByOwner])
}
```

**Чего в карточке нет.** Из профиля НР не переносятся `maxSlots` и `maxProjects` (про руководство проектом, не про консультацию) и `status` / `moderationComment` (карточка не модерируется). Из первоначальной постановки не берутся формат консультации и лимит запросов в месяц — по решению заказчика.

**ФИО** отдельным полем не дублируется — берётся из `User.name`: одна учётная запись, одно имя в обоих пространствах.

**Учёное звание и степень необязательны.** У НР они обязательны, но внешний эксперт из индустрии может не иметь ни того ни другого — поэтому здесь это поля со значением по умолчанию, а не обязательные.

**Обязательный минимум для попадания в каталог** (`cardCompleted = true`): место работы, должность, **резюме**, доменная экспертиза — хотя бы один тег, контакт. Резюме обязательно по решению заказчика: смысл раздела в том, что у эксперта подтверждённый опыт, и резюме — это подтверждение. Фото остаётся опциональным. Остальное тоже опционально.

Отсутствие резюме сервер отклоняет с ошибкой; остальные поля минимума проверяются на клиенте, а их отсутствие даёт `cardCompleted = false` — карточка сохранится, но в каталог не попадёт.

Условие показа в каталоге: `cardCompleted && !hiddenByOwner`. Два флага вместо одного нужны, чтобы различать «ещё не заполнил» и «заполнил, но скрыл»: сообщения пользователю в этих случаях разные.

**Дубль с профилем НР.** Фото, место работы, должность, звание, степень и резюме есть и в профиле НР, и в карточке эксперта. Иначе нельзя: у внешних экспертов профиля НР не существует вовсе. При подключении роли карточка предзаполняется из профиля НР, дальше правится отдельно и живёт своей жизнью.

### 5.3 Запрос на консультацию

```prisma
enum ExpertRequestStatus {
  NEW                     // Новый — ждёт модератора
  NEEDS_REVISION          // На доработке — модератор вернул студенту с комментарием (M1)
  APPROVED_BY_MODERATOR   // Одобрен модератором — ждёт эксперта
  REJECTED_BY_MODERATOR   // Отклонён модератором (терминальный)
  REJECTED_BY_EXPERT      // Отклонён экспертом (терминальный)
  CONTACTS_SENT           // Контакты отправлены
  AWAITING_FEEDBACK       // Ожидает обратной связи
  CLOSED                  // Закрыт
}

model ExpertRequest {
  id                String   @id @default(cuid())
  studentId         String
  student           StudentProfile @relation(fields: [studentId], references: [id], onDelete: Cascade)
  expertId          String
  expert            ExpertProfile  @relation(fields: [expertId], references: [id], onDelete: Cascade)

  // Анкета (08.12)
  topic             String   // Что хочу обсудить — от 500 символов
  expectedResult    String   // Какой результат жду от встречи — от 80 символов
  ownProgress       String   // Что уже сделал, чтобы разобраться — от 500 символов
  problemArea       String?  // Где возникли проблемы — необязательно
  materialsUrl      String?  // Ссылка на папку с материалами — необязательно
  projectId         String?  // Проект студента на платформе — необязательно

  // Снимок профиля на момент запроса: курс и программа со временем меняются,
  // а модератор и эксперт должны видеть то, что было при отправке
  directionSnapshot String
  courseSnapshot    Int

  status            ExpertRequestStatus @default(NEW)
  moderatorComment  String?  // комментарий модератора студенту: обязателен при возврате и отклонении, по желанию при одобрении (M1)
  expertComment     String?  // причина отказа эксперта

  moderatedById     String?
  moderatedAt       DateTime?
  acceptedAt        DateTime?
  contactsSentAt    DateTime?  // точка отсчёта 7 дней
  feedbackRequestedAt     DateTime?
  feedbackReminderSentAt  DateTime?
  closedAt          DateTime?
  closedWithoutFeedback Boolean @default(false) // закрыт джобой, а не студентом (08.20)

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  feedbacks         ExpertRequestFeedback[]

  @@index([status])
  @@index([expertId, status])
  @@index([studentId])
  @@index([status, contactsSentAt])       // для джобы 7 дней
  @@index([status, feedbackRequestedAt])  // для джобы напоминания
}
```

Требование 08.18 выполняется конструктивно: экспертные запросы — отдельная сущность `ExpertRequest`, существующая модель `Application` не затрагивается, ни один список не объединяет их.

### 5.4 Обратная связь

```prisma
enum FeedbackAuthorSide {
  STUDENT
  EXPERT
}

model ExpertRequestFeedback {
  id          String   @id @default(cuid())
  requestId   String
  request     ExpertRequest @relation(fields: [requestId], references: [id], onDelete: Cascade)
  authorSide  FeedbackAuthorSide

  metHappened Boolean          // состоялась ли встреча
  rating      Int?             // 1–5, только от студента
  comment     String?

  createdAt   DateTime @default(now())

  @@unique([requestId, authorSide])   // по одному ответу с каждой стороны
  @@index([requestId])
}
```

Имя намеренно не `Feedback` — эта модель в схеме уже занята тикетами поддержки.

Два ответа на один и тот же вопрос «встреча была?» дают честную метрику 08.19: ответа только студента недостаточно, потому что при сорвавшейся встрече студенты такие формы не заполняют.

**Три категории в метрике конверсии**, а не две:

- *встреча состоялась* — есть подтверждение хотя бы одной стороны;
- *встреча не состоялась* — есть ответ, и он отрицательный;
- *нет данных* — запрос закрыт джобой без ответа студента (`closedWithoutFeedback = true`).

Смешивать третью категорию со второй нельзя: молчуны систематически занизят конверсию, и метрика перестанет отражать реальность.

### 5.5 Открытые когорты

```prisma
model ExpertPipelineCohortAccess {
  id           String   @id @default(cuid())
  cohort       String   @unique   // значение из справочника cohorts
  isOpen       Boolean  @default(false) // Экспертная труба
  platformOpen Boolean  @default(false) // Платформа ВКР (A1)
  updatedAt    DateTime @updatedAt
}
```

Имя модели осталось от первой версии, когда таблица обслуживала только трубу: переименование таблицы ради названия не стоит отдельной миграции.

Отдельная таблица вместо записи в общих настройках — проще индексировать, проще смотреть историю и не надо парсить JSON при каждой проверке доступа.

---

## 6. Статусная машина

| Из | Кто переводит | В | Условия и эффекты |
|----|---------------|---|-------------------|
| — | Студент | `NEW` | Когорта открыта, анкета валидна, к этому эксперту нет нерассмотренного запроса (`NEW`, `NEEDS_REVISION`, `APPROVED_BY_MODERATOR`). Уведомление модератору |
| `NEW` | Модератор | `APPROVED_BY_MODERATOR` | Комментарий по желанию, его видит студент (M1). Уведомление эксперту |
| `NEW` | Модератор | `NEEDS_REVISION` | Комментарий обязателен. Уведомление и письмо студенту с комментарием (M1) |
| `NEEDS_REVISION` | Студент | `NEW` | Студент исправил анкету: те же проверки, что при отправке, эксперт тот же и должен быть в каталоге. Снимок программы и курса обновляется. Комментарий возврата остаётся для модератора. Уведомление модератору «Запрос исправлен после доработки». Число возвратов не ограничено (M1) |
| `NEW` | Модератор | `REJECTED_BY_MODERATOR` | Причина обязательна. Уведомление студенту с причиной. Терминальный |
| `NEEDS_REVISION` | Модератор | `REJECTED_BY_MODERATOR` | Причина обязательна. Выход для запроса, который не исправляют или который нельзя отправить, потому что эксперт скрыл карточку (M1) |
| `APPROVED_BY_MODERATOR` | Эксперт | `CONTACTS_SENT` | Принятие и отправка контактов — один переход в одной транзакции. `acceptedAt`, `contactsSentAt`. Обмен контактами |
| `APPROVED_BY_MODERATOR` | Эксперт | `REJECTED_BY_EXPERT` | Уведомление студенту. Терминальный |
| `CONTACTS_SENT` | Система (джоба) | `AWAITING_FEEDBACK` | `contactsSentAt + 7 дней`. Формы обеим сторонам, `feedbackRequestedAt` |
| `AWAITING_FEEDBACK` | Студент | `CLOSED` | Ответ студента получен, `closedAt` |
| `AWAITING_FEEDBACK` | Система (джоба) | `CLOSED` | `feedbackRequestedAt + 7 дней`, ответа студента нет. `closedWithoutFeedback = true`. Уведомления не отправляются |

**Атомарность решений модератора (M1).** Переход выполняется условным обновлением: статус меняется, только если запрос в разрешённом исходном статусе и не менялся с момента загрузки очереди (`updatedAt` карточки уходит вместе с решением). Иначе — 409 «Запрос изменился — обновите очередь», и уведомления не отправляются. Так из устаревшей вкладки нельзя одобрить анкету, которую студент уже переписал, и два одновременных решения не проходят оба. Очередь сортируется по последнему изменению: исправленный запрос поднимается наверх с пометкой «исправлен <дата>».

**Эксперт скрыл карточку, запрос на доработке.** Страница исправления сразу объясняет, что эксперт не принимает запросы, вместо анкеты, которую сервер всё равно не примет. Запрос остаётся на доработке до возвращения эксперта; модератор может его отклонить.

**Права на переходы.** Модератор — только `ADMIN`. Решение по запросу — только эксперт, которому запрос адресован (сверка `expertId` с `ExpertProfile` текущего пользователя, не с `userId`). Закрытие — только студент-автор запроса.

**Что видит эксперт до принятия:** анкету, имя студента и его программу. Контакты студента не показываются. Запросы в статусах `NEW` и `NEEDS_REVISION` эксперту не видны: входящие строятся по явному списку статусов `EXPERT_VISIBLE_STATUSES`, а не «всё, кроме» (M1). Комментарий модератора эксперту не отдаётся.

**Что видит студент:** свои запросы со статусами, комментарий модератора (при возврате — с кнопкой «Исправить запрос», при отклонении — как причину, при одобрении — если модератор его оставил), причину отказа эксперта, контакты эксперта — только после принятия.

---

## 7. API

| Метод и путь | Кто | Назначение |
|--------------|-----|------------|
| `GET /api/expert/access` | авторизованный | Состояние плиток для экрана выбора |
| `POST /api/expert/join` | НР | Подключить роль эксперта |
| `GET`/`PUT /api/expert/profile` | эксперт | Карточка: чтение, сохранение, скрытие |
| `GET /api/expert/catalog` | студент | Каталог, фильтр по темам |
| `GET /api/expert/catalog/[id]` | студент | Карточка без контактов |
| `POST /api/expert/requests` | студент | Отправить анкету |
| `PATCH /api/expert/requests/[id]` | студент-автор | Исправить запрос на доработке и отправить снова (M1) |
| `GET /api/expert/requests` | студент / эксперт | Свои запросы (набор полей зависит от роли) |
| `POST /api/expert/requests/[id]/decision` | эксперт | Принять или отклонить |
| `POST /api/expert/requests/[id]/feedback` | студент / эксперт | Обратная связь |
| `GET /api/expert/admin/requests` | админ | Очередь модерации |
| `POST /api/expert/admin/requests/[id]/moderate` | админ | `approve` (комментарий по желанию), `return` на доработку и `reject` (комментарий обязателен) — M1 |
| `GET`/`PUT /api/expert/admin/access` | админ | Открытые когорты + счётчик студентов без потока |
| `POST /api/expert/admin/invitations` | админ | Пригласить внешнего эксперта |
| `GET /api/expert/admin/metrics` | админ | Конверсия запрос → встреча |
| `POST /api/expert/jobs/feedback` | cron | Джобы 7 дней и напоминания |

Контакты эксперта не должны попадать в ответ `/api/expert/catalog*` ни при каких условиях — фильтрация на уровне `select`, а не на клиенте.

Приглашение внешнего эксперта переиспользует существующую логику создания аккаунта (генерация пароля, письмо с доступами, запись `Invitation`), но кнопка живёт в админке трубы. Проверка домена почты, которая применяется к студентам, к экспертам не применяется — у них корпоративная почта.

---

## 8. Уведомления

| Событие | Кому | Тип | Email |
|---------|------|-----|-------|
| Новый запрос | Всем `ADMIN` | `EXPERT_REQUEST_NEW` | Нет |
| Запрос исправлен после доработки | Всем `ADMIN` | `EXPERT_REQUEST_NEW` | Нет |
| Возвращён на доработку | Студенту, с комментарием | `EXPERT_REQUEST_RETURNED` | Да |
| Запрос одобрен модератором | Эксперту | `EXPERT_REQUEST_APPROVED` | Да |
| Отклонён модератором | Студенту, с причиной | `EXPERT_REQUEST_REJECTED` | Да |
| Отклонён экспертом | Студенту, с причиной | `EXPERT_REQUEST_REJECTED` | Да |
| Принят экспертом | Студенту — контакты эксперта | `EXPERT_REQUEST_ACCEPTED` | Да |
| Принят экспертом | Эксперту — контакты студента | `EXPERT_REQUEST_ACCEPTED` | Да |
| Прошло 7 дней | Студенту и эксперту | `EXPERT_FEEDBACK_REQUEST` | Да |
| Студент не ответил 3 дня | Студенту | `EXPERT_FEEDBACK_REQUEST` | Да |

In-app — через существующий `notify()` / `notifyMany()`, email — через `sendMail()`. Модераторов может быть несколько: рассылка по всем пользователям с ролью `ADMIN`.

Все ссылки в уведомлениях ведут внутрь `/expert/*` напрямую, минуя экран выбора (08.04).

### 8.1 Привязка уведомлений к пространству (08.22)

Уведомления двух пространств не смешиваются: счётчик в сайдбаре и список на странице показывают только то, что относится к пространству, в котором человек находится. На переключателе пространств — бейдж «есть непрочитанные в другом пространстве», чтобы ничего не потерялось.

Пространство **не хранится в модели** `Notification` и миграция не нужна — оно вычисляется из типа уведомления по явному списку `EXPERT_NOTIFICATION_TYPES` в `lib/notification-space.ts`: типы трубы относятся к ней, остальные к платформе. `EXPERT_REQUEST_RETURNED` (M1) внесён в список — иначе уведомление о возврате ушло бы в платформу, закрытую первокурснику.

**У каждого пространства своя страница**, а не общая с параметром: `/notifications` для платформы и `/expert/notifications` для трубы. Так защита получается структурной — страница трубы лежит внутри её route-группы и наследует гейт доступа, а не полагается на то, что параметр не забудут передать. Общий список вынесен в компонент, дублирования кода нет.

`GET /api/notifications` принимает `space` и возвращает вместе со списком два счётчика: непрочитанные в текущем пространстве и в другом. Отметка «прочитать все» действует только на текущее пространство.

Оговорка на будущее: при добавлении новых значений в `NotificationType` их нужно явно относить к пространству. Дефолт для незнакомого типа — платформа, чтобы уведомление не потерялось.

Фактически разделение заметят только научный руководитель с ролью эксперта и админ: у студента и у внешнего эксперта уведомления всегда из одного пространства.

---

## 9. Отложенные задачи

Повторяем существующий паттерн `app/api/events/reminders/route.ts`: POST-роут, защищённый `CRON_SECRET`, вызывается с сервера по расписанию.

`POST /api/expert/jobs/feedback?secret=...` — два прохода за один вызов:

1. **Сбор обратной связи.** Запросы в `CONTACTS_SENT` с `contactsSentAt <= now - 7 дней` → `AWAITING_FEEDBACK`, формы студенту и эксперту, `feedbackRequestedAt = now`.
2. **Напоминание.** Запросы в `AWAITING_FEEDBACK` с `feedbackRequestedAt <= now - 3 дня`, без ответа студента и с пустым `feedbackReminderSentAt` → одно напоминание, `feedbackReminderSentAt = now`.
3. **Авто-закрытие.** Запросы в `AWAITING_FEEDBACK` с `feedbackRequestedAt <= now - 7 дней` и без ответа студента → `CLOSED`, `closedWithoutFeedback = true`, `closedAt = now`. Уведомления не отправляются. Ответ эксперта, если он есть, сохраняется и учитывается в метрике.

**Таймлайн запроса после принятия:** контакты отправлены → +7 дней форма обратной связи → +3 дня напоминание студенту → +7 дней от формы авто-закрытие. У студента есть 4 дня после напоминания.

Идемпотентность обеспечивают сами отметки времени: повторный вызов в тот же день ничего не задублирует. Джоба безопасна при пропуске дня — просто догонит накопившееся.

Запуск: запись в crontab прод-сервера, раз в сутки, рядом с ночными бэкапами.

---

## 10. Затрагиваемое легаси

| Файл | Что меняется |
|------|--------------|
| `prisma/schema.prisma` | `EXPERT` в `Role`, значения в `NotificationType`, связь `User.expert`, пять новых моделей |
| `lib/auth.ts` | Ветка `trigger === "update"` перечитывает признак роли эксперта; признак попадает в JWT и сессию |
| `middleware.ts` | `/expert` в защищённые маршруты; гейт `profileCompleted` для роли `EXPERT` ведёт на карточку эксперта, а не на профиль НР; блокировка трубы по когорте и пространства ВКР для внешних экспертов |
| `app/login/page.tsx` | Редирект после логина — на `/spaces` вместо развилки по ролям |
| `app/page.tsx` | Авторизованный на корне → `/spaces` |
| `components/layout/AppSidebar.tsx` | Переключатель пространств; сайдбар трубы; не показывать сайдбар платформы внешнему эксперту; счётчик уведомлений по текущему пространству + бейдж о непрочитанных в другом |
| `middleware.ts` | Из списка разрешённого внешнему эксперту убраны `/notifications`: у трубы теперь своя страница уведомлений |
| `app/api/notifications/route.ts` | Параметр пространства в выборке и в подсчёте непрочитанных (08.22) |
| `app/notifications/page.tsx` | Фильтрация списка по пространству (08.22) |
| `app/api/admin/invitations/route.ts` | Снять бинарность `STUDENT`/`SUPERVISOR` — принять роль `EXPERT` |
| `app/admin/dictionaries/page.tsx` | Защита от переименования когорт с привязанными студентами (раздел 4) |
| `app/{projects,supervisors,applications,calendar,knowledge,my-projects,nir}/layout.tsx` | Новые: гейт платформы по потоку студента (A1) |
| `app/api/notifications/route.ts` | Студенту закрытого потока в пространстве платформы — только ответы поддержки (A1) |
| `app/api/onboarding/route.ts` | Закрытому потоку — пустые шаги (A1) |
| `components/layout/AppSidebar.tsx` | У закрытого потока в сайдбаре платформы только профиль, уведомления и обращения (A1) |
| `app/api/{projects,supervisors,applications,events,favorites,knowledge,students}/**` | Первой строкой обработчиков, доступных студенту, — `denyVkrClosed()`: закрытый поток и внешний эксперт (A1, 08.02) |
| `lib/knowledge.ts` | Видимость базы знаний закрыта по умолчанию для ролей, кроме админа, студента и научника (08.02) |
| `app/profile/student/page.tsx` | После первого заполнения профиля — на `/spaces` вместо `/my-projects` (A1) |

Новое: `app/spaces/`, `app/expert/**`, `app/api/expert/**`.

---

## 11. Требует подтверждения

Открытых вопросов по разделу нет: состав анкеты согласован и реализован (08.12).

**Файлы к запросу не прикладываются.** Вместо загрузки — одна ссылка на папку с материалами: презентация, литобзор, ноутбук с кодом, черновик работы. Решение заказчика, поэтому отдельная таблица под файлы не понадобилась.
