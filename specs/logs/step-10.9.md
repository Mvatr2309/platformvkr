# Step 10.9 — Журнал изменений

## Запись #1 — Фикс C1: закрытие GET /api/projects/[id] от анонимного доступа

**Запрос:** Аудит безопасности выявил уязвимость C1 — `GET /api/projects/[id]` не проверял авторизацию и отдавал email/контакты участников и НР. Любой анонимный пользователь мог перебором id выкачивать персональные данные.

**Реализация:**
- Добавлена проверка авторизации в начало GET-хендлера через `requireAuth()` из `lib/api-guard.ts`.
- `requireProjectAccess` намеренно НЕ использован: он запрещает доступ не-участникам, что сломало бы просмотр проекта студентами для подачи заявки (email автора показывается откликающимся by design).
- Остаточный риск (контакты участников команды видны любому авторизованному пользователю) отнесён к M2 — отдельная задача минимизации PII.

**Файлы:**
- `app/api/projects/[id]/route.ts` — импорт `requireAuth, isGuardError`; guard в начале GET.

**Проверка:** `npx tsc --noEmit` — новых ошибок в изменённом файле нет (присутствующие ошибки `favoriteSupervisor`/`.next` предсуществующие, не связаны).

**Коммит:** (не закоммичено — ожидает запроса пользователя)

## Запись #2 — Выделение контакта научного руководителя при рассмотрении заявки

**Запрос:** При рассмотрении студентом заявки от научного руководителя контакты («Контакт: VK, TG: @...») отображались тусклым серым цветом — почти не видны. Нужно выделить.

**Реализация:**
- В блоке «Инфо о НР-заявителе» контакт переведён с серого класса `.contact` на заметный `.contactRevealed` (синий текст #1565C0 на фоне #E3F2FD, отступы), значение обёрнуто в `<strong>`. Стиль переиспользован из уже существующего в этом же модуле.

**Файлы:**
- `app/applications/page.tsx:651` — класс контакта НР изменён на `contactRevealed`.

**Коммит:** (не закоммичено — ожидает запроса пользователя)

## Запись #3 — Фикс «Ошибка подачи заявки» при предложении проекта руководителю

**Запрос:** Студентка Панфилова Т.П. не могла предложить свой проект Зверевой А.К. — выходила «Ошибка подачи заявки».

**Причина:** ограничение БД `@@unique([projectId, studentId])` на `Application` разрешает студенту только ОДНУ заявку на проект (любой тип/любой НР). У проекта уже было активное `SUPERVISION_REQUEST` (PENDING) к Атаевой О.М. от 12.05.2026. Предварительная проверка дубликата в коде смотрела только пару (проект + конкретный НР), пропускала запрос к `prisma.create`, а тот падал на нарушении constraint → пустой `catch` отдавал 500 «Ошибка подачи заявки».

**Реализация:**
1. (данные) Удалена устаревшая заявка Атаевой `cmp2fsfkq001xyvwr4j7rvd41` (SUPERVISION_REQUEST/PENDING) на проде — освободило unique-слот.
2. (код) Проверка дубликата в ветке SUPERVISION_REQUEST расширена: ищем любую заявку студента на проект и возвращаем понятный 409 (тому же НР / другому НР с именем / иная активная заявка) вместо падения в 500.
3. (код) В `catch` POST добавлен `console.error` — раньше причина 500 глушилась.

**Файлы:**
- `app/api/applications/route.ts` — расширена проверка дубликата SUPERVISION_REQUEST; логирование в catch.

**Проверка:** `npx tsc --noEmit` — ошибок в файле нет.

**Коммит:** 723e81e (задеплоено)

## Запись #4 — Разрешить предлагать проект нескольким научным руководителям

**Запрос:** Студент должен иметь возможность предложить один проект сразу нескольким НР (раньше из-за `@@unique([projectId, studentId])` — только одному за раз).

**Реализация:**
1. (схема) `Application.@@unique([projectId, studentId])` → `@@unique([projectId, studentId, supervisorId])`. Миграция `20260615120000_allow_multiple_supervision_requests` (DROP старого индекса + CREATE нового).
2. (код, POST /api/applications) ветка SUPERVISION_REQUEST: проверка дубликата теперь по тройке (проект+студент+конкретный НР); добавлен лимит `MAX_SUPERVISION_REQUESTS_PER_PROJECT = 3` активных предложений на проект.
3. (код) STUDENT-ветка: `findUnique({ projectId_studentId })` заменён на `findFirst({ projectId, studentId, type: "STUDENT" })` (составной ключ удалён сменой constraint).
4. Авто-отзыв прочих предложений при выборе одного НР уже был реализован ранее (`student_accept`, updateMany → DECLINED). UI «Мои предложения» уже поддерживал список. Изменений не потребовали.

**Файлы:**
- `prisma/schema.prisma` — изменён `@@unique` на Application.
- `prisma/migrations/20260615120000_allow_multiple_supervision_requests/migration.sql` — новая миграция.
- `app/api/applications/route.ts` — проверка дубликата/лимит SUPERVISION_REQUEST; STUDENT findFirst.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 915e024 (задеплоено, миграция применена)

## Запись #5 — Предупреждение об авто-отзыве при выборе руководителя

**Запрос:** Показывать предупреждение, что остальные предложения будут отозваны.

**Реализация:** в разделе «Мои предложения» при подтверждении НР (статус AWAITING_STUDENT) считается число других активных предложений по тому же проекту. Если их >0 — текст-подсказка и диалог `confirm()` явно предупреждают «остальные предложения (N) будут автоматически отозваны». Если предложение единственное — лишний текст не показывается.

**Файлы:**
- `app/applications/page.tsx` — блок AWAITING_STUDENT обёрнут в IIFE с подсчётом `otherActive`; подсказка и confirm стали условными.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 43f69b9 (задеплоено)

## Запись #6 — Разбор проблем пользователей: добавление тиммейтов, потеря ссылки/участника, кнопка удаления

**Запрос:** разобрать 3 проблемы пользователей и внести исправления.

**Проблема 1 — нельзя добавить тиммейта (роль блокирует):** фронт требовал роль обязательно, бэк — нет; список ролей пуст, если у проекта нет requiredRoles (45% проектов на проде).
→ Роль сделана необязательной в `handleAddManualMember`; плейсхолдер «Роль (необязательно)».

**Проблема 2 — теряется ссылка при создании:** ссылка попадала в проект только через кнопку «Добавить ссылку»; введённая, но не добавленная — терялась на сабмите.
→ В `handleSave` авто-добавление непустой ссылки (`linkUrl`) перед отправкой; валидация http(s) с явной ошибкой вместо потери.

**Проблема 3А — теряется участник при создании:** та же причина для `memberForm`.
→ В `handleSave` авто-добавление заполненного участника; при частично заполненных полях — явная ошибка, данные не теряются.

**Проблема 3Б — пропадает кнопка «Удалить проект»:** by design (нельзя удалить с участниками), но без объяснения.
→ Показывается неактивная кнопка «Удалить» с подсказкой «Сначала удалите участников команды».

**Проблема 3В («взлом»):** не уязвимость — авторизация в members/[memberId] и DELETE /projects/[id] корректна, привилегии не повышаются. Без изменений.

**Файлы:**
- `app/projects/[id]/page.tsx` — роль необязательна; неактивная кнопка удаления с подсказкой.
- `app/projects/new/page.tsx` — авто-коммит ссылки и участника в `handleSave`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 26930ac (задеплоено)

## Запись #7 — Лимит размера команды (3 участника) + предупреждение

**Запрос:** ограничить команду — автор + 2 тиммейта = 3 человека (НР не считается); показывать предупреждение при попытке добавить больше.

**Реализация:**
- Бэкенд `manual-members` POST: добавлен `_count.members` и проверка `>= MAX_TEAM_MEMBERS (3)` → 400 с понятным текстом. Раньше лимита при ручном добавлении не было.
- Согласован лимит стартапов в заявках: `STARTUP_TEAM_LIMIT` 4→3 (`applications/[id]/route.ts`), inline-проверка `>= 4`→`>= 3` (`applications/route.ts`).
- Фронтенд `projects/[id]/page.tsx`: константа `MAX_TEAM_MEMBERS=3`; `teamFull` (для не-исследований); guard в `handleAddManualMember`; кнопка «+ Добавить» становится неактивной при полной команде + текст-предупреждение под заголовком секции участников.

**Данные:** на проде 2 проекта уже имеют по 4 участника (созданы до лимита) — не изменялись, новые добавления в них блокируются.

**Файлы:**
- `app/api/projects/[id]/manual-members/route.ts`, `app/api/applications/[id]/route.ts`, `app/api/applications/route.ts`, `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** e31756c (задеплоено)

## Запись #8 — Роль тиммейта снова обязательна, но список не пустеет

**Запрос:** вернуть обязательность роли (отмена части записи #6), но чтобы список ролей не исчезал и всегда был доступен для выбора.

**Причина исходной проблемы:** выпадашка строилась из `openRoles` (требуемые роли минус занятые) → пустела по мере заполнения и была пустой у проектов без requiredRoles.

**Реализация (`app/projects/[id]/page.tsx`):**
- В `handleAddManualMember` роль снова обязательна.
- Новый `roleOptions`: полный список требуемых ролей проекта (без вычитания занятых), а если роли у проекта не заданы — глобальный справочник `roles` (на проде 8 значений). Выпадашка тиммейта использует `roleOptions` вместо `openRoles`.
- Плейсхолдер вернулся на «Выберите роль *».
- Список откликов студента (apply-форма) по-прежнему использует `openRoles` — там «свободные слоты» осмысленны, не меняли.

**Файлы:** `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 8e90773 (задеплоено)

## Запись #9 — Привязка добавленного по почте студента к его аккаунту

**Запрос:** при добавлении участника по почте он помечается «не в системе», даже если студент уже зарегистрирован, и не видит проект.

**Причина:** `manual-members` всегда создавал участника с `inSystem:false` и `manualEmail`, без связи со `studentId` → проект не попадал в «Мои проекты» студента (выборка идёт по `studentId`/`userId`).

**Реализация (`app/api/projects/[id]/manual-members/route.ts`):**
- Перед созданием ищем `StudentProfile` по email пользователя с ролью STUDENT.
- Если найден — участник создаётся со связью `studentId` и `inSystem:true` (manual-поля не заполняются); студенту шлётся уведомление «Вас добавили в проект».
- Если не найден — как раньше, ручной участник.

**Данные (прод):** существующие ручные участники, чья почта совпала с зарегистрированным студентом: 22 привязаны к аккаунтам (studentId, inSystem=true, manual-поля очищены), 3 удалены как дубли (студент уже был настоящим участником проекта). Несвязанных совпадений не осталось (0).

**Файлы:** `app/api/projects/[id]/manual-members/route.ts`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 74599cc (задеплоено)

## Запись #10 — Предупреждение при добавлении студента, которого нет в системе

**Запрос:** при добавлении участника по почте, если такого студента нет в базе — показывать предупреждение.

**Реализация:**
- Бэкенд `manual-members`: принимает флаг `allowExternal`. Поиск студента по почте сделан регистронезависимым (`mode: "insensitive"`). Если студент не найден и `allowExternal` не передан → 409 `{ notInSystem: true, error: "Студента с такой почтой нет в системе…" }`.
- Фронтенд `projects/[id]/page.tsx`: `handleAddManualMember` → `submitManualMember(allowExternal)`. При ответе `notInSystem` показывается `confirm` с текстом предупреждения; при согласии — повторная отправка с `allowExternal: true` (добавление как внешнего участника), при отказе — ничего (можно исправить опечатку).

**Файлы:** `app/api/projects/[id]/manual-members/route.ts`, `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 7354ecc (задеплоено)

## Запись #11 — Автоподбор студента в форме добавления участника

**Запрос:** автоподбор существующего студента по мере ввода имени/почты (дополняет предупреждение из #10).

**Реализация:**
- Новый эндпоинт `GET /api/students/search?q=` — поиск зарегистрированных студентов по имени/почте (регистронезависимо), доступ STUDENT/SUPERVISOR/ADMIN, мин. 2 символа, до 8 результатов, возвращает name/email/direction.
- Фронтенд `projects/[id]/page.tsx`: ввод в поля «ФИО»/«E-mail» запускает дебаунс-поиск (250мс), под полями показывается выпадающий список подсказок; выбор подставляет ФИО+почту+магистратуру. Подсказка «Начните вводить имя или почту…». Не противоречит #10 — предупреждение про «нет в системе» остаётся для ручного ввода/отсутствующих студентов.

**Файлы:** `app/api/students/search/route.ts` (новый), `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** c4b22bf (задеплоено)

## Запись #12 — Восстановление ФИО после привязки (регресс из #9)

**Запрос:** у привязанных в #9 участников пропало ФИО.

**Причина:** в миграции #9 при привязке к аккаунту был очищен `manualName`. У студентов с незаполненным профилем `user.name` пустой → имя не отображалось.

**Реализация:**
1. (данные) Восстановил `manualName` из бэкапа `vkr_20260609_104725.sql.gz` через временную таблицу — 5 из 13 (остальные добавлены после 9 июня, в бэкапе их нет).
2. (фронт) Фоллбэк имени участника: `user.name → manualName → user.email → "—"`. У 8 невосстановимых теперь показывается почта вместо пустоты; имя появится после заполнения профиля.
3. (бэкенд) При привязке нового участника к аккаунту теперь сохраняем `manualName` (ФИО от автора) как запасное имя — чтобы регресс не повторялся.

**Файлы:** `app/projects/[id]/page.tsx`, `app/api/projects/[id]/manual-members/route.ts`. Плюс правка данных на проде.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 60266c9 (задеплоено)

## Запись #13 — Запрет добавления внешних участников (только из базы)

**Запрос:** в команду можно добавлять только зарегистрированных студентов; если почты нет в базе — добавить нельзя (доступы открываются админами вручную).

**Реализация:**
- Бэкенд `manual-members`: убран путь `allowExternal`/`notInSystem`; если студент по почте не найден → 400 с понятным текстом, участник не создаётся. Создание всегда со связью `studentId` (`inSystem:true`).
- Фронтенд `projects/[id]/page.tsx`: `submitManualMember` без подтверждения внешнего добавления — просто показывает ошибку.

**Данные:** на проде остаётся 21 «внешний» участник (studentId IS NULL, manualEmail) — добавлены ранее. Не трогал, требуется решение пользователя (удалять/оставить).

**Файлы:** `app/api/projects/[id]/manual-members/route.ts`, `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** f19728b (задеплоено)

## Запись #14 — Удаление внешних участников + обязательный домен @phystech.edu для студентов

**Запрос:** удалить всех внешних участников; почты студентов должны быть @phystech.edu, с предупреждением и запретом.

**Данные:** удалён 21 внешний участник (studentId IS NULL, manualEmail), авторов среди них не было. Осталось 0.

**Реализация (домен только для STUDENT; НР — любая почта):**
- Хелпер `lib/student-email.ts`: `STUDENT_EMAIL_DOMAIN="phystech.edu"`, `isStudentEmailAllowed()`, `STUDENT_EMAIL_ERROR`.
- `register`: при role===STUDENT и не-@phystech.edu → 400.
- `admin/invitations` (одиночное): при STUDENT и не-@phystech.edu → 400.
- `admin/invitations/bulk`: не-@phystech.edu адреса студентов помечаются invalid («только @phystech.edu»).
- UI админ-приглашений: подсказка про @phystech.edu при выборе роли «Студент».
- Откатил давний незакоммиченный стрей-дифф в `admin/invitations/route.ts` (requireAdmin сохранён).

**Файлы:** `lib/student-email.ts` (новый), `app/api/auth/register/route.ts`, `app/api/admin/invitations/route.ts`, `app/api/admin/invitations/bulk/route.ts`, `app/admin/invitations/page.tsx`. Плюс правка данных.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** 0b30213 (задеплоено)

## Запись #15 — Скрыть «1 на 1» в исследованиях и число заявок от студентов

**Запрос:** убрать текст «1 на 1 с научным руководителем» в списке проектов для исследований (не показывать кол-во человек); скрыть число заявок проекта от студентов.

**Реализация:**
- `app/projects/page.tsx`: для CLASSIC_DISSERTATION убран бейдж «1 на 1…» (ничего не показываем). Для остальных «· N заявок» рендерится только при `role !== "STUDENT"`.
- `app/api/projects/route.ts`: сессия поднята наверх GET; для роли STUDENT в ответе `_count.applications=0` (чтобы число не утекало через API). Восстановлен случайно удалённый ранее `revalidatePath` (стрей-правка).

**Файлы:** `app/projects/page.tsx`, `app/api/projects/route.ts`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #16 — Фикс: автор задваивается и становится 4-м участником

**Запрос:** иногда автор проекта дублируется и становится 4-м человеком в команде (пример — Шараф Халиль).

**Причина:** в POST `/api/projects` автор-студент добавляется отдельной записью как создатель (`isCreator=true`, со `studentId`), а затем цикл по `data.members` создаёт ручных тиммейтов. Дедуп `seenEmails` работал только внутри списка тиммейтов и не учитывал почту автора. Если автор вписывал себя в список команды, появлялась вторая запись (`studentId=null`, его `manualEmail`) → 4 участника вместо 3.

**Реализация:**
- `app/api/projects/route.ts`: перед циклом добавляем `session.user.email` в `seenEmails`, чтобы собственная запись автора в списке тиммейтов пропускалась.

**Данные (прод):** удалены 2 дубля автора (Шараф Халиль `sharaf.kh@phystech.edu`, Царюк Артём `tsariuk.av@phystech.edu`) — записи `studentId IS NULL`, совпадающие по почте с автором-создателем. Запись создателя сохранена.

**Файлы:** `app/api/projects/route.ts`. Плюс правка данных.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #17 — Привязка тиммейтов к аккаунтам при создании проекта

**Запрос:** при создании проекта тиммейты-студенты должны искаться в базе и привязываться к аккаунтам (как в форме добавления участника, запись #13), а не записываться как «не в системе».

**Причина:** логику привязки/запрета внешних из #13 применили только к `manual-members/route.ts` (форма на странице готового проекта). Роут создания (`POST /api/projects`) по-прежнему писал всех тиммейтов как `inSystem:false`, `studentId=null` — поэтому добавленный студент не видел проект в «Моих проектах».

**Реализация (`app/api/projects/route.ts`):**
- Перед созданием проекта тиммейты валидируются: дедуп по почте, исключение самого автора, поиск `StudentProfile` по почте (role STUDENT, case-insensitive).
- Если кто-то не найден в системе → 400 со списком таких почт, проект не создаётся (политика «только зарегистрированные», #13).
- Найденные создаются со связью `studentId`, `inSystem:true`, `manualName` как запасное имя.

**Файлы:** `app/api/projects/route.ts`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #18 — Фикс дашборда: две «программы» по управлению IT-продуктом

**Запрос:** в дашборде показываются две программы — «управление IT-продуктом» и «управление IT продуктами».

**Причина:** блок «Научные руководители по направлениям» (`app/api/admin/dashboard/route.ts`) берёт справочник `directions`, а затем добавляет отдельной строкой любые значения из профилей НР, которых нет в справочнике (route.ts:153). У 56 НР направление было записано как «Управление IT продуктом» (ед. число), которого в справочнике нет → вылезало второй строкой рядом с каноничным «Управление IT продуктами».

**Данные (прод, без правки кода):**
- 5 НР имели оба варианта → удалён «Управление IT продуктом» (array_remove), чтобы не было дубля в массиве.
- 51 НР → «Управление IT продуктом» заменён на «Управление IT продуктами» (array_replace).
- Итог: осталось «Управление IT продуктами» (63) и «Разработка IT-продуктов» (40).

**Файлы:** правка данных в `SupervisorProfile.directions`. Кода не меняли.

**На будущее:** профили НР допускают значения направлений вне справочника — стоит нормализовать ввод (валидация/выбор из справочника), иначе расхождения написания повторятся.

## Запись #19 — Защита от «задвоения» участника проекта (триггер БД)

**Запрос:** студент `mamedrzaev.zsh@phystech.edu` задвоился — проверить и перестраховаться, чтобы такого не происходило.

**Причина:** в самой базе дубля нет (один `User`, один `StudentProfile`). «Задвоение» — это ручная запись участника проекта (`inSystem=false`, email в `manualEmail`), не связанная с его аккаунтом, хотя студент зарегистрирован. Создана старым билдом 16.06 в 12:42 — фикс логики (#13, коммиты `74599cc`/`f19728b`) был в репо с 10:07, но прод ещё не пересобрали этой версией.

**Данные (прод):** привязал к аккаунтам 3 осиротевшие ручные записи — Мамедрзаев З.Ш. (`mamedrzaev.zsh`), Алексеев К.В. (`alekseev.kv`), Кубатаев М.А. (`kubataev.ma`): проставлен `studentId`, `inSystem=true`, очищен `manualEmail`. Осиротевших записей, совпадающих с зарегистрированными юзерами, осталось 0.

**Страховка:** триггер БД `trg_normalize_project_member` (BEFORE INSERT/UPDATE на `ProjectMember`) — если `manualEmail` совпадает с email зарегистрированного студента, БД сама проставляет `studentId`, `inSystem=true` и очищает `manualEmail`. Ловит любой путь вставки, включая старые билды. Применён на проде напрямую + сохранён как миграция.

**Файлы:** `prisma/migrations/20260622120000_normalize_project_member/migration.sql`. Плюс правка данных на проде.

**Коммит:** (ожидает деплоя)

## Запись #20 — Сохранение фильтров на странице «Научные руководители»

**Запрос:** на `/supervisors` при выборе фильтров, открытии профиля НР и возврате назад фильтры сбрасываются. Нужно, чтобы они сохранялись, в т.ч. для дальнейших поисков.

**Причина:** фильтры (поиск + направление + звание + тип проекта + статус набора) хранились только в локальном `useState` (`app/supervisors/page.tsx`). Карточка НР — обычная ссылка `<a href="/supervisors/{id}">` (полная навигация), поэтому переход в профиль и кнопка «назад» пересоздают страницу-список и обнуляют состояние. Нигде не было персистентности (ни URL, ни хранилище).

**Реализация (`app/supervisors/page.tsx`):**
- Константа `FILTERS_STORAGE_KEY = "supervisors-filters"`.
- `useEffect` на маунте читает сохранённые фильтры из `localStorage` и восстанавливает их (try/catch на случай недоступного хранилища); флаг `filtersLoaded`.
- `useEffect` пишет фильтры в `localStorage` при каждом изменении, но только после `filtersLoaded`, чтобы пустые дефолты не затёрли сохранённое.
- Кнопка «Сбросить» по-прежнему очищает фильтры (пустые значения сохраняются как сброшенное состояние).
- Покрывает оба сценария: возврат из профиля и новая сессия («для дальнейших поисков»).

**Файлы:** `app/supervisors/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #21 — Фикс: выделение контакта в «Уведомлениях» уводит со страницы

**Запрос:** научный руководитель выделяет текст контакта в «Уведомлениях» (чтобы скопировать/написать), и его выкидывает из раздела.

**Причина:** в `app/notifications/page.tsx` вся карточка уведомления — один кликабельный `<div onClick={handleClick}>`, а `handleClick` при наличии `link` делает `window.location.href = notif.link`. По завершении выделения текста браузер генерирует событие `click` на этом `<div>` → срабатывает навигация. У НР есть уведомление «Контакт студента» (`app/api/applications/[id]/route.ts:144`), где контакт лежит в тексте сообщения, а `link: /applications` — поэтому при попытке выделить контакт уносит на `/applications`. Баг общий для любого уведомления со ссылкой, контакты просто единственное, что хочется выделять.

**Реализация (`app/notifications/page.tsx`):** в начале `handleClick` — ранний выход, если есть выделение: `if (window.getSelection()?.toString()) return;`. Обычный клик по карточке по-прежнему открывает ссылку и помечает прочитанным; выделение/копирование текста больше не навигирует.

**Файлы:** `app/notifications/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #22 — Кнопка «Скопировать контакт» в уведомлениях

**Запрос:** добавить явную кнопку копирования контакта в контактные уведомления (продолжение #21).

**Контекст:** контакт зашит прямо в текст сообщения, отдельного поля в модели `Notification` нет, тип у обоих уведомлений — `APPLICATION_ACCEPTED`. Выбран фронтовый парсинг (без миграции БД) — кнопка работает и на уже существующих уведомлениях, и на новых; при изменении формулировки просто не показывается (graceful degradation).

**Реализация (`app/notifications/page.tsx`, `notifications.module.css`):**
- Хелпер `extractContact(message)` достаёт контакт двумя регэкспами: «Контакт студента Имя: <контакт>. Договоритесь…» (НР) и «…Контакт: <контакт>» в конце строки (студент). Проверено на e-mail, Telegram, телефоне и составных контактах.
- Состояние `copiedId`, функция `copyContact` (`navigator.clipboard.writeText`, фолбэк — ручное выделение всё ещё работает после #21).
- Кнопка показывается только когда контакт распознан; `e.stopPropagation()`, чтобы не сработала навигация карточки. Текст меняется на «Контакт скопирован ✓» на 2 сек.
- Стиль `.copyBtn` — корал из дизайн-системы.

**Файлы:** `app/notifications/page.tsx`, `app/notifications/notifications.module.css`.

**Проверка:** `npx tsc --noEmit` — ошибок нет; регэксп протестирован на 6 формулировках.

**Коммит:** (ожидает деплоя)

## Запись #23 — E-mail НР о новом предложении проекта от студента

**Запрос:** НР `ashemakhin@yandex.ru` не получил письмо. Диагностика: почта исправна (Yandex SMTP, ошибок в логах нет), но событие «студент предложил проект НР» (`SUPERVISION_REQUEST`) слало только in-app, без e-mail. Решено добавить письмо на это событие.

**Реализация (`app/api/applications/route.ts`):**
- Импортирован `sendMail`.
- В POST-ветке `SUPERVISION_REQUEST` в выборку `supervisor` добавлены `user.name`/`user.email`.
- После in-app `notify` («Предложение проекта») добавлена отправка письма НР: тема «Вам предложили проект — {title}», тело по стилю остальных писем (корал-кнопка → `/applications`). Обёрнуто в `.catch` с логом, e-mail некритичен.

**Файлы:** `app/api/applications/route.ts`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #24 — Показ ошибки при назначении НР (нет свободных слотов)

**Запрос:** студент `petrenko.se@phystech.edu` жал «Назначить руководителем» (заявка НР Атаевой) — ничего не происходило. Диагностика: у Атаевой лимит проектов 4/4, сервер отклоняет назначение с 400, но фронт ошибку не показывал (`handleAction` реагировал только на успех).

**Реализация:**
- `app/api/applications/[id]/route.ts` (accept для SUPERVISOR-заявки): переформулирована ошибка лимита под студента — «У научного руководителя нет свободных слотов: занято N из M проектов. Назначить его руководителем нельзя — выберите другого или свяжитесь с ним.» (с реальными числами `supProjectCount`/`maxProjects`).
- `app/applications/page.tsx` (`handleAction`): обёрнут в `try/catch`; при `!res.ok` читается `data.error` и показывается в красной плашке (`error` state); при сетевом сбое — запасной текст. Чинит весь класс «ничего не происходит», не только слоты.
- `applications.module.css`: добавлен стиль `.errorMsg` (корал `#E8375A`, светлый фон, левый бордер).

**Файлы:** `app/api/applications/[id]/route.ts`, `app/applications/page.tsx`, `app/applications/applications.module.css`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #25 — Профиль студента для модератора (клик по почте)

**Запрос:** как модератор, хочу нажимать на почту студента и открывать его профиль с проектами — чтобы при запросе студента на платформе видеть контекст: в каких проектах и на каких ролях он состоит, и кто его сокомандники.

**Контекст:** в админке email студента показывался как текст (обращения `/admin/feedback`, список `/admin/students-list`, аккаунты `/admin/students`), страницы профиля студента у админа не было. Все данные уже в БД (`User → StudentProfile → ProjectMember → Project`), схема не менялась. Страница сделана по паттерну `admin/moderation/[id]`.

**Реализация:**
- `app/api/admin/students/[id]/route.ts` (новый): GET-профиль студента под `requireAdmin` — данные пользователя и анкеты + список проектов (название, статус, роль «Автор»/роль участника, научный руководитель, дата вступления) + сокомандники каждого проекта (имя, email, роль, признак «не в системе»; сам студент из списка исключён). 404, если пользователь не найден или не студент.
- `app/admin/students/[id]/page.tsx` + `detail.module.css` (новые): страница профиля — карточки «Основная информация» (магистратура, курс, когорта, контакт, портфолио, дата регистрации), «Компетенции»/«Желаемые роли», «О себе»; ниже — карточки проектов со статус/роль-бейджами, ссылкой на проект и списком сокомандников (кликабельны, если есть аккаунт). Кнопка «← Назад» через `router.back()`.
- Кликабельный email (ведёт на `/admin/students/{userId}`): `/admin/students-list` и `/admin/students` — только для студентов «в системе» (у ручных участников аккаунта нет), `/admin/feedback` — только для авторов-студентов (в `app/api/feedback/route.ts` в выборку `user` добавлен `id`).

**Файлы:** `app/api/admin/students/[id]/route.ts`, `app/admin/students/[id]/page.tsx`, `app/admin/students/[id]/detail.module.css`, `app/admin/students-list/page.tsx`, `app/admin/students/page.tsx`, `app/admin/feedback/page.tsx`, `app/api/feedback/route.ts`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #26 — Сортировка кликом по заголовкам колонок во всех списках админки

**Запрос:** (1) фильтр «Статус ЛК» на списке студентов различал только «В системе»/«Не в системе», хотя статусов три; (2) сортировка кликом по заголовку «Статус ЛК» как у ФИО; (3) расширено пользователем — сделать кликабельными ВСЕ колонки во всех разделах-списках админки.

**Контекст:** сортировка по одной колонке была только в трёх списках (студенты/НР — ФИО, проекты — Название), в остальных отсутствовала. Логика везде дублировалась инлайном.

**Реализация:**
- `lib/useTableSort.ts` (новый): хук `useTableSort` (поле+направление, `toggleSort`, стрелка ↑/↓ у активной колонки) и `compareValues` (числа/строки-ru, пустые значения всегда в конце). Вторичная сортировка везде — по ФИО/названию.
- Фильтр «Статус ЛК» на `/admin/students-list` стал трёхпозиционным со счётчиками: В системе / Профиль не заполнен / Не в системе (по логике бейджей колонки).
- Кликабельные заголовки: `students-list` (7 колонок; Статус ЛК: В системе → Профиль не заполнен → Не в системе), `supervisors-list` (8), `projects-list` (7; Статус по жизненному циклу DRAFT→…→COMPLETED, Дата хронологически), `accounts` (5; Роль по русским названиям), `invitations` (4; Статус: Отправлено → Создан → Истекло), `workload` (все; Загрузка/Прогресс по доле занятых слотов, селект сортировки сохранён и синхронизирован с заголовками), `students` (5; Роль в проекте по первой роли).
- `invitations.module.css`: у `th` курсор-указатель и hover (в `list.module.css` уже было).

**Файлы:** `lib/useTableSort.ts`, `app/admin/students-list/page.tsx`, `app/admin/supervisors-list/page.tsx`, `app/admin/projects-list/page.tsx`, `app/admin/accounts/page.tsx`, `app/admin/invitations/page.tsx`, `app/admin/invitations/invitations.module.css`, `app/admin/workload/page.tsx`, `app/admin/students/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #27 — Модерация проекта прямо со страницы проекта

**Запрос:** модератор проваливается в проект из раздела «Модерация», изучает описание и артефакты, но одобрить/отклонить с комментарием может только вернувшись в раздел и заново найдя проект. Нужно давать решение прямо в открытом проекте, если он подан на модерацию.

**Контекст:** API `PUT /api/admin/projects/[id]` (approve/reject + комментарий → смена статуса, лента активности, in-app и email уведомления) уже существует и используется очередью `/admin/projects`. На странице проекта `isAdmin` и `project.status` уже вычислялись — не хватало только UI.

**Реализация (`app/projects/[id]/page.tsx`):**
- Панель «Модерация проекта» под шапкой (над «Описанием»), видна только админу и только при статусе PENDING: поле «Комментарий модератора (необязательно)» + кнопки «Одобрить»/«Отклонить» (вид и поведение — как в очереди модерации), выделена синей рамкой.
- `handleModeration(action)` вызывает существующий API; при успехе — refetch проекта и ленты активности (бейдж статуса обновляется, панель исчезает), зелёная плашка «Проект одобрен и открыт для заявок» / «Проект отклонён и возвращён в черновики»; при ошибке — красный текст в панели.
- API и раздел «Модерация» не менялись, старый путь работает как раньше.

**Файлы:** `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #28 — Файлы проектов: скачивание только участникам, НР и админу

**Запрос:** файлы проектов должны скачивать только участники команды проекта, научные руководители (все) и администратор; студенты не из проекта — не могут.

**Контекст:** файлы лежали в `public/uploads/projects/` и раздавались статикой — скачать мог кто угодно по прямому URL даже без авторизации. Права проверялись только на списке файлов в API, не на самих файлах.

**Реализация:**
- `app/api/projects/[id]/files/[fileId]/download/route.ts` (новый): единственный путь скачивания. `requireAuth`; ADMIN и SUPERVISOR (любой) — доступ, STUDENT — только участник проекта (по `ProjectMember`), иначе 403. Отдаёт файл с оригинальным именем (`Content-Disposition: attachment`), ищет в приватной папке с fallback на legacy `public/`.
- `app/api/projects/[id]/files/route.ts`: загрузка — в приватную `uploads/projects/` (вне public), квота и удаление резолвят оба расположения (`storedFileCandidates`). Формат `filepath` в БД не менялся.
- `app/projects/[id]/page.tsx`: «Скачать» → новый роут; флаг `canDownloadFiles = isAdmin || SUPERVISOR || isMember`; без прав — надпись «Доступно участникам» с тултипом. Внешние ссылки (LINK) не тронуты.
- `.gitignore`: `/uploads/`.
- Миграция на проде: `mv public/uploads/projects/* uploads/projects/` + проверка, что nginx не раздаёт `/uploads` статикой.

**Файлы:** `app/api/projects/[id]/files/[fileId]/download/route.ts`, `app/api/projects/[id]/files/route.ts`, `app/projects/[id]/page.tsx`, `.gitignore`.

**Проверка:** `npx tsc --noEmit` — ошибок нет; после деплоя — прямой URL мёртв, роут без сессии отдаёт 401.

**Коммит:** (ожидает деплоя)

## Запись #29 — Раздел «НИР» для студентов (оболочка с заглушками)

**Запрос:** новый раздел «НИР» у студента: внутри страница с менюшкой-квадратиками «НИР 3» и «НИР 4» для будущей сдачи файлов. По решению пользователя внутри плиток пока заглушка «Раздел в проработке» — формы сдачи будут отдельной задачей.

**Контекст:** упоминаний НИР в проекте не было — фича новая. Утверждённый план на будущее (для форм): файлы НИР привязаны к проекту/команде, сдаёт команда (несколько файлов свободно, название + файл), скачивают участники проекта, научный руководитель этого проекта и админ; хранение приватное. БД/API в этой итерации сознательно не создавались.

**Реализация:**
- `app/nir/page.tsx` + `nir.module.css`: страница «НИР» с двумя квадратными плитками 220×220 (цифра, подпись, hover с синей рамкой) по дизайн-системе.
- `app/nir/[semester]/page.tsx`: `/nir/3` и `/nir/4` — заголовок, «← НИР», заглушка «Раздел в проработке»; иные семестры — notFound().
- `components/layout/Header.tsx`: пункт «НИР» для роли STUDENT (десктоп + мобильное меню).
- `middleware.ts`: `/nir` в protectedPaths.

**Файлы:** `app/nir/page.tsx`, `app/nir/[semester]/page.tsx`, `app/nir/nir.module.css`, `components/layout/Header.tsx`, `middleware.ts`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #30 — Фикс: «НИР» не появлялся в меню студента + позиция после «Профиля»

**Запрос:** после деплоя #29 студент не видел пункт «НИР»; также поставить «НИР» сразу после «Профиля».

**Диагностика:** в #29 пункт был добавлен в `components/layout/Header.tsx`, но это **мёртвый компонент** — его никто не импортирует (первая версия гипотезы про «испорченный кэш сборки» не подтвердилась: и после `rm -rf .next` чанки не содержали «НИР», а строка «Мои заявки» в бандле пришла из `app/applications/page.tsx`). Реальная навигация — сайдбар `components/layout/AppSidebar.tsx`, подключённый в `app/layout.tsx`.

**Реализация:**
- `components/layout/AppSidebar.tsx`: в группе «Моё» студента пункт «НИР» добавлен сразу после «Профиль» (Профиль | НИР | Мои проекты | Заявки).
- `components/layout/Header.tsx`: мои правки из #29/#30 откатаны — файл возвращён к исходному виду (мёртвый код, менять его не следовало).

**Файлы:** `components/layout/AppSidebar.tsx`, `components/layout/Header.tsx` (откат).

**Проверка:** `npx tsc --noEmit` — ошибок нет; после деплоя чанк сайдбара содержит «НИР»/`/nir`.

**Коммит:** (ожидает деплоя)

## Запись #31 — Подписи на плитках НИР

**Запрос:** на карточках раздела «НИР» сверху вниз: крупная цифра (как есть), ниже «семестр», ещё ниже «Сдача отчета».

**Реализация:** `app/nir/page.tsx` — подписи плиток заменены: «НИР 3»/«Сдача файлов» → «семестр»/«Сдача отчета» (аналогично для 4). Стили без изменений.

**Файлы:** `app/nir/page.tsx`.

**Проверка:** `npx tsc --noEmit` — ошибок нет.

**Коммит:** (ожидает деплоя)

## Запись #32 — Фикс: «Файл не найден на диске» у файлов проектов на модерации

**Запрос:** при скачивании файлов проектов на модерации — ошибка «Файл не найден на диске».

**Диагностика:** недосмотр в #28 — у файлов проектов два пути загрузки. Страница проекта (`/api/projects/[id]/files`) была переведена на приватное хранилище, а форма создания проекта грузит через `/api/upload` (type=project) → файлы падали в публичную `public/uploads/` с путём `/uploads/<имя>` (без `projects/`). Download-роут такой путь не резолвит → 404; вдобавок эти файлы оставались публично доступными по прямому URL (защита #28 на них не действовала). Проекты на модерации — свежесозданные, у них все файлы такие. Масштаб: 60 файлов на диске, 46 записей в БД.

**Реализация:**
- `app/api/upload/route.ts`: для type=project файл пишется в приватную `uploads/projects/`, возвращается путь `/uploads/projects/<имя>`; photo/resume остаются в публичной папке (by design).
- Миграция на проде: `mv public/uploads/*-project-* uploads/projects/` + SQL `UPDATE "ProjectFile" SET filepath = replace(filepath, '/uploads/', '/uploads/projects/') WHERE filepath LIKE '/uploads/%' AND filepath NOT LIKE '/uploads/projects/%'`.

**Файлы:** `app/api/upload/route.ts` + миграция данных на сервере.

**Проверка:** `npx tsc --noEmit` — ошибок нет; после деплоя: в БД 0 ненормализованных путей, старый прямой URL — 404, download-роут без сессии — 401.

**Коммит:** (ожидает деплоя)

## Запись #33 — База знаний: типы материалов, видимость по ролям, визуальный редактор, закрытые файлы

**Запрос:** админ добавляет материалы двух форматов — свёрстанная статья или просто файл для скачивания (с задаваемым названием файла); видимость каждого материала настраивается отдельно для студентов и отдельно для НР.

**Реализация:**
- `prisma/schema.prisma` + миграция `20260821120000_knowledge_visibility_and_file_materials`: enum `MaterialType` (ARTICLE/FILE), у `Article` — `type`, `visibleToStudents`, `visibleToSupervisors` (default true — старые статьи видны всем), `content` стал default "".
- `lib/knowledge.ts`: помощники видимости (`knowledgeVisibilityWhere`, `isArticleVisible`).
- `app/api/knowledge/route.ts`: GET теперь требует авторизацию (раньше — нет) и фильтрует по роли; POST принимает тип и флаги видимости, для FILE контент не обязателен.
- `app/api/knowledge/[id]/route.ts`: GET скрывает статью от роли без доступа (404); PUT принимает флаги видимости.
- `app/api/knowledge/[id]/files/route.ts`: файлы пишутся в приватную `uploads/knowledge/` (вместо `public/`); поле `title` из формы — отображаемое имя файла (расширение сохраняется).
- Новый `app/api/knowledge/[id]/files/[fileId]/download/route.ts`: скачивание с проверкой авторизации и видимости статьи по роли, fallback на легаси-папку (зеркально файлам проектов).
- `components/RichTextEditor.tsx` + css: WYSIWYG на TipTap (Ж/К, H2/H3, списки, ссылки, undo/redo); хранит HTML — совместим со старыми статьями. Зависимости: @tiptap/react, @tiptap/pm, @tiptap/starter-kit.
- `app/knowledge/page.tsx`: форма создания — выбор формата (статья/файл), редактор, файл + его название, чекбоксы видимости; карточка материала-файла скачивается сразу из списка (админ переходит на страницу управления), бейджи видимости для админа.
- `app/knowledge/[id]/page.tsx`: редактор в форме правки, чекбоксы видимости, загрузка файла с названием, скачивание через защищённый роут.

**Файлы:** см. выше + `app/knowledge/knowledge.module.css`, `package.json`.

**Проверка:** `npx tsc --noEmit` — чисто; `next build` локально: компиляция и генерация 67 страниц ✓ (финальный шаг standalone-копирования виснет из-за iCloud-синка Desktop — проблема окружения, не кода; продовая сборка на сервере).

**Деплой:** git pull + npm install + SQL миграции вручную через psql + `mv public/uploads/knowledge/* uploads/knowledge/` + npm run build + pm2 restart vkr.

## Запись #34 — Запрет создания проектов научными руководителями + миграция их проектов в «Предлагаемые темы»

**Запрос:** НР больше не создают собственные проекты; существующие проекты, созданные НР, удалить, предварительно перенеся их названия в раздел «Предлагаемые темы» профиля НР. Перед удалением проверить, что в этих проектах нет студентов.

**Реализация:**
- `app/api/projects/route.ts`: POST — 403 для роли SUPERVISOR (ветка автопривязки supervisorId и лимита maxProjects стала недостижимой, оставлена без изменений).
- `app/projects/page.tsx`, `app/my-projects/page.tsx`: кнопка «Создать проект» скрыта для НР.
- `app/projects/new/page.tsx`: для НР вместо формы — сообщение «Научные руководители не создают проекты...» с кнопкой в профиль.
- Права админа и механика руководства студенческими проектами (назначение/подтверждение НР) не тронуты. Поле maxProjects в профиле НР оставлено — убрать отдельной задачей.

**Миграция данных на проде:** 10 проектов от 4 НР (Пастухова ×3, Шемахин ×2, Корчинская ×2, Тимошилов ×3); у всех подтверждено 0 участников и 0 заявок (проверка повторена в транзакции удаления). Названия перенесены в proposedTopics (существующий текст сохранён, темы дописаны снизу через пустую строку); проекты удалены каскадом; 2 осиротевших PDF удалены из uploads/projects. Бэкап всех удалённых записей — локально (backup-sup-projects.json).

**Файлы:** `app/api/projects/route.ts`, `app/projects/page.tsx`, `app/my-projects/page.tsx`, `app/projects/new/page.tsx` + миграция данных на сервере.

**Проверка:** `npx tsc --noEmit` — чисто; после деплоя: POST /api/projects под НР → 403, кнопок создания у НР нет, в БД 0 проектов НР без создателя, темы в профилях на месте.

## Запись #35 — Дашборд админа: клик по цифре ведёт на соответствующий отфильтрованный список

**Запрос:** плитки «Требует внимания» и «Проекты» вели на общие списки; клик по значению должен открывать именно тот список, по которому посчитана цифра.

**Реализация:**
- `app/admin/dashboard/page.tsx`: «Профилей НР на модерации» → /admin/moderation; «Проектов без НР» → /admin/projects-list?filter=no_supervisor; «Проектов без участников» → ?filter=no_members; «Студентов без проекта» → /admin/students-list?filter=no_project; «Новых обращений» → /admin/feedback?status=NEW; плитки «Проекты» (Всего/Открытых/Активных/Завершённых) стали ссылками на projects-list с ?status=. «Проектов на модерации» уже вёл верно (/admin/projects) — не тронут.
- `app/admin/projects-list/page.tsx`: чтение ?status= и ?filter= из URL (useSearchParams + Suspense по образцу supervisors-list); спец-фильтры no_supervisor/no_members зеркалят формулы дашборда (не-DRAFT); плашка «Фильтр: … ✕» со сбросом.
- `app/admin/students-list/page.tsx`: ?filter=no_project — студенты с профилем без участия в проектах; плашка со сбросом.
- `app/admin/feedback/page.tsx`: начальный statusFilter из ?status= (селект статуса уже был).
- `app/admin/list.module.css`, `dashboard.module.css`: стили плашки фильтра и hover кликабельных плиток.

**Файлы:** см. выше.

**Проверка:** `npx tsc --noEmit` — чисто; после деплоя переходы с дашборда открывают списки с применённым фильтром и плашкой.

## Запись #36 — Автор проекта может закрывать набор участников

**Запрос:** дать автору проекта возможность самому закрывать/открывать набор в свой проект отдельным признаком.

**Реализация (подход «флаг + бейдж», не новый статус жизненного цикла):**
- `prisma/schema.prisma` + миграция `20260827120000_project_recruitment_closed`: `Project.recruitmentClosed` Boolean default false (у существующих проектов набор открыт).
- `app/api/projects/[id]/route.ts` (PUT): ветка `recruitmentClosed` — переключать могут только автор проекта и админ (403 остальным, включая НР-руководителя); пишется запись в ленту активности.
- `app/api/applications/route.ts`: заявка студента на проект при закрытом наборе → 400 «Набор в проект закрыт». Предложение проекта НР и заявка НР на руководство не блокируются.
- `app/api/projects/route.ts` (список): при закрытом наборе openRoles=[] — бейдж ролей и фильтр «только с открытыми ролями» отрабатывают автоматически.
- `app/projects/[id]/page.tsx`: кнопка «Закрыть набор»/«Возобновить набор» (автор/админ, только для статуса «Открыт»), бейдж «Набор закрыт», блок «Подать заявку» у студентов заменяется сообщением.
- `app/projects/page.tsx`: бейдж «Набор закрыт» на карточке вместо «Открытые роли»/«Ищет студента».
- CSS: `.recruitmentClosedBadge` в project.module.css.

**Файлы:** см. выше.

**Проверка:** `npx tsc --noEmit` — чисто; после деплоя миграция применена вручную через psql.

## Запись #37 — Фикс отображения: один бейдж «Набор закрыт» вместо пары с «Открыт»

**Запрос:** на странице проекта при закрытом наборе показывались два «статуса» рядом («Открыт» + «Набор закрыт») — выглядит противоречиво.

**Реализация:** `app/projects/[id]/page.tsx` — при статусе OPEN и закрытом наборе бейдж статуса заменяется единственным бейджем «Набор закрыт» (для всех ролей, включая админа). Внутренний статус проекта не меняется.

**Файлы:** `app/projects/[id]/page.tsx`.

**Проверка:** `npx tsc --noEmit` — чисто.

## Запись #38 — База знаний: книги (многосоставные материалы с главами и страницами)

**Запрос:** возможность добавлять в базу знаний «книги» с главами и страницами внутри; оглавление всегда доступно читателю.

**Реализация (вариант «дерево в Article», обсуждён и одобрен):**
- `prisma/schema.prisma` + миграция `20260828120000_knowledge_books`: self-relation `parentId` (каскадное удаление) + `sortOrder`, в `MaterialType` добавлен `BOOK`. Существующие материалы — корневые, миграция данных не нужна.
- `app/api/knowledge/route.ts`: список отдаёт только корни (главы/страницы не в каталоге), поиск находит и страницы; POST с `parentId` создаёт главу/страницу (только под книгой/главой, максимум 3 уровня, sortOrder автоинкремент, категория и видимость копируются от родителя).
- `app/api/knowledge/[id]/route.ts`: GET отдаёт дерево книги (для оглавления) и проверяет видимость по корню; PUT принимает `sortOrder`, изменение видимости/категории книги каскадно применяется к главам и страницам.
- `app/knowledge/[id]/page.tsx`: макет книги — постоянное оглавление сбоку (десктоп, sticky, текущая страница подсвечена), кнопка «☰ Оглавление» на мобильном, кнопки «← предыдущая / следующая →»; титульная страница книги; у глав/страниц категория и видимость не редактируются (наследуются). Панель «Структура книги» для админа: + глава, + страница, ↑/↓, удаление с подтверждением.
- `app/knowledge/page.tsx`: третий формат «Книга» в форме создания (описание — необязательно; после создания переход на страницу книги), карточка книги с числом глав.
- `knowledge.module.css`: стили оглавления, навигации, панели структуры, мобильная версия.

**Файлы:** см. выше.

**Проверка:** `npx tsc --noEmit` — чисто; сборка и миграция на проде при деплое.

## Запись #38.1 — Белая подложка контента в базе знаний

**Запрос:** у текстовых страниц книги контент рендерился без фоновой подложки (на сером фоне), в отличие от оглавления рядом.

**Реализация:** класс `.contentCard` (белый фон + рамка + отступы) на контентную колонку книги и на просмотр обычных статей — вся база знаний единообразна.

**Файлы:** `app/knowledge/[id]/page.tsx`, `app/knowledge/knowledge.module.css`.

## Запись #39 — Спека раздела «Экспертная труба» (FR-08)

**Запрос:** спроектировать новый раздел платформы: студент находит эксперта и отправляет запрос на консультацию, модератор и эксперт проверяют запрос, после двух подтверждений стороны обмениваются контактами, через 7 дней система собирает обратную связь. Договориться об архитектуре до написания кода.

**Согласованные решения (по шагам, с апрувом на каждом):**
- Навигация: экран выбора пространства `/spaces` («Платформа ВКР» / «Экспертная труба») при каждом заходе на сайт, сессия не сбрасывается; диплинки из писем и уведомлений минуют экран; постоянный переключатель в сайдбаре; контекст в URL-префиксе `/expert/*`, платформа остаётся на текущих адресах (перенос сломал бы отправленные ссылки).
- Админка: полная симметрия — у трубы своя админ-зона `/expert/admin/*`. Модератор = существующая роль `ADMIN`, новой роли модератора нет.
- Три состояния плитки: открыто / замок (некликабельна) / приглашение подключить роль эксперта.
- Роли: роль эксперта = наличие `ExpertProfile`, поле `User.role` не перезаписывается (иначе НР теряет научное руководство). НР подключает роль сам, мгновенно, без модерации, через существующий механизм `updateSession()`. Внешние эксперты — новое значение `EXPERT` в `Role`, аккаунты создаёт админ из админки трубы, доступа к «Платформе ВКР» у них нет. Выхода из роли нет — только скрытие карточки.
- Доступ студентов: белый список когорт из существующего справочника `cohorts`, галочки в админке трубы. Отдельной «даты открытия» нет. Пустая когорта = нет доступа + счётчик таких студентов в админке. Проверка на сервере, не только в UI.
- Отложенные задачи: паттерн существующего `app/api/events/reminders/route.ts` — POST-роут под `CRON_SECRET`, идемпотентность по отметкам времени, запуск из crontab прода.

**Реализация:** только документ. `specs/08-FR-08-expert-pipeline.md` — 19 требований (08.01–08.19), роли и доступы, навигация и URL-карта, когортный гейт, модель данных (`ExpertProfile`, `ExpertRequest`, `ExpertRequestFeedback`, `ExpertPipelineCohortAccess` + правки `Role`/`NotificationType`), статусная машина с правами на переходы, карта API, матрица уведомлений, джобы, таблица затрагиваемого легаси, 8 пунктов «требует подтверждения» (состав полей карточки и анкеты, лимиты, судьба запроса без обратной связи, название раздела для студентов).

**Кода и миграций нет** — по договорённости этот шаг только проектный.

**Файлы:** `specs/08-FR-08-expert-pipeline.md` (новый), `specs/logs/step-10.9.md`.

**Проверка:** не требуется — изменений в коде нет.

## Запись #39.1 — FR-08: отказ от лимитов на запросы и авто-закрытие без обратной связи

**Запрос:** лимиты на отклики не делать вообще; запрос авто-закрывается спустя неделю, если обратной связи нет.

**Реализация (правки `specs/08-FR-08-expert-pipeline.md`):**
- Новые требования: 08.20 (авто-закрытие без обратной связи), 08.21 (система не ограничивает количество запросов ни у студента, ни у эксперта).
- Поле `monthlyLimit` в карточке эксперта оставлено как справочная цифра для студента — система её не проверяет.
- Вместо лимитов единственная защита при создании запроса: нельзя отправить второй запрос тому же эксперту, пока первый не рассмотрен.
- Статусная машина: переход `AWAITING_FEEDBACK` → `CLOSED` системой через 7 дней от запроса обратной связи, флаг `closedWithoutFeedback` в `ExpertRequest` отличает его от закрытия студентом. Уведомления при авто-закрытии не отправляются.
- Джоба: третий проход (авто-закрытие). Зафиксирован таймлайн: контакты → +7 дней форма → +3 дня напоминание → +7 дней от формы закрытие (у студента 4 дня после напоминания).
- Метрика 08.19: три категории вместо двух — «состоялась», «не состоялась», «нет данных». Авто-закрытые без ответа идут в третью, иначе молчуны занижают конверсию.
- Раздел «Требует подтверждения» сократился с 8 пунктов до 5: закрыты вопросы про лимит эксперта, лимиты студента и судьбу запроса без обратной связи.

**Файлы:** `specs/08-FR-08-expert-pipeline.md`.

**Проверка:** не требуется — изменений в коде нет.

## Запись #39.2 — FR-08: состав полей карточки эксперта, отказ от транзитного статуса

**Запрос:** поля карточки эксперта — такие же, как у научного руководителя, только вместо «Предлагаемых тем» опциональное «С какими темами могу помочь». Формат консультации и лимит запросов в месяц не пишем, типы проектов оставить, «Готов работать с» убрать, «Направления студентов» оставить. Поля подгружаются из профиля НР и дальше правятся отдельно. Название раздела — «Экспертная труба».

**Реализация (правки `specs/08-FR-08-expert-pipeline.md`):**
- Раздел 5.2 переписан: `ExpertProfile` повторяет набор полей `SupervisorProfile` — место работы, должность, учёное звание, учёная степень, резюме, фото, доменная экспертиза, направления студентов, типы проектов, контакт одним полем. Вместо `proposedTopics` — `helpTopics` («С какими темами могу помочь», опционально).
- Не переносятся: `maxSlots`, `maxProjects` (про руководство), `status` / `moderationComment` (карточка не модерируется), `workPreferences` («Готов работать с»). Удалены из черновика: `ExpertFormat` и формат консультации, `monthlyLimit`, три раздельных поля контактов.
- Учёное звание и степень сделаны необязательными: у внешнего эксперта из индустрии их может не быть, тогда как у НР они обязательны.
- Зафиксирован обязательный минимум для попадания в каталог: место работы, должность, хотя бы один тег экспертизы, контакт.
- Предзаполнение карточки при подключении роли расширено: фото, место работы, должность, звание, степень, резюме, экспертиза, направления, типы проектов. Дальше карточка живёт отдельно от профиля НР.
- Убран транзитный статус `ACCEPTED_BY_EXPERT`: принятие и отправка контактов — один переход в одной транзакции, факт принятия фиксирует `acceptedAt`. Перечисление статусов сократилось до семи значений.
- Название «Экспертная труба» зафиксировано для всех ролей, включая студентов.
- Требование 08.10 уточнено: фильтр каталога — по тегам доменной экспертизы.
- Раздел «Требует подтверждения» сократился с 5 пунктов до 2: остались состав полей анкеты с порогом длины и уведомления в двух пространствах.

**Файлы:** `specs/08-FR-08-expert-pipeline.md`.

**Проверка:** не требуется — изменений в коде нет.

## Запись #39.3 — FR-08: уведомления привязаны к пространству

**Запрос:** уведомления двух пространств не смешивать — счётчик и список показывают только текущее пространство.

**Реализация (правки `specs/08-FR-08-expert-pipeline.md`):**
- Новое требование 08.22: счётчик и список уведомлений по текущему пространству, бейдж «есть непрочитанные в другом» на переключателе пространств.
- Новый подраздел 8.1: пространство не хранится в модели `Notification` и миграция не нужна — вычисляется из типа (`EXPERT_*` → труба, остальное → платформа). Нужны функция-маппинг, параметр пространства в `GET /api/notifications` и фильтр на странице. Оговорка: новые значения `NotificationType` относить к пространству явно, дефолт для незнакомого типа — платформа.
- Таблица затрагиваемого легаси пополнилась `app/api/notifications/route.ts` и `app/notifications/page.tsx`; у `AppSidebar.tsx` добавлен пункт про счётчик и бейдж.
- Раздел «Требует подтверждения» сократился до одного пункта: состав полей анкеты запроса и порог длины.

**Файлы:** `specs/08-FR-08-expert-pipeline.md`.

**Проверка:** не требуется — изменений в коде нет.

## Запись #40 — FR-08, этап 1: модель данных экспертной трубы и роль эксперта в сессии

**Запрос:** начать реализацию раздела «Экспертная труба» по спеке. Этап 1 — схема, миграция, признак роли эксперта в сессии. UI не трогаем.

**Реализация:**
- `prisma/schema.prisma` (+132 строки, ни одной удалённой): `EXPERT` в `Role`; связь `User.expert`; `StudentProfile.expertRequests`; пять значений `EXPERT_*` в `NotificationType`; модели `ExpertProfile`, `ExpertRequest`, `ExpertRequestFeedback`, `ExpertPipelineCohortAccess`; перечисления `ExpertRequestStatus`, `FeedbackAuthorSide`.
- Миграция `20260922120000_expert_pipeline`: 4 таблицы, 2 перечисления, 6 новых значений в существующих перечислениях, 11 индексов, 4 внешних ключа. Только добавление — ни `DROP`, ни `DELETE`, ни `TRUNCATE`, поэтому на проде она безопасна.
- `lib/auth.ts`: `isExpert` в JWT и сессии — вычисляется как наличие `ExpertProfile`. Ветка `trigger === "update"` перечитывает признак из БД, что даёт подключение роли эксперта без повторного входа (08.05). Поле `User.role` не трогается: у НР оно занято `SUPERVISOR`.
- `types/next-auth.d.ts`, `types/roles.ts`: `isExpert` в `Session`, `User`, `JWT`; значение `EXPERT` в `UserRole`.

**Локальная среда:** база поднята в Docker (`docker compose up -d db`) и залита копией прода (`pg_dump -Fc` → `scp` → `pg_restore`, md5 сверен). На проде в `_prisma_migrations` не записаны 4 вручную накатанные миграции — локально они помечены применёнными через `prisma migrate resolve --applied`. На проде эта нестыковка остаётся и всплывёт при деплое: вопрос вынесен пользователю, правки не вносились.

**Важно для дальнейшей работы:** `prisma migrate dev` на этой базе запускать нельзя — она видит расхождение схемы прода с историей миграций и сбрасывает базу (проверено на практике, локальная копия была обнулена и восстановлена из дампа). Рабочий путь: `prisma migrate diff` для генерации SQL + `prisma migrate deploy` для применения.

**Файлы:** `prisma/schema.prisma`, `prisma/migrations/20260922120000_expert_pipeline/migration.sql`, `lib/auth.ts`, `types/next-auth.d.ts`, `types/roles.ts`, `specs/08-FR-08-expert-pipeline.md`.

**Проверка:** `npx tsc --noEmit` — чисто; миграция применена локально, дифф схемы и базы пустой; данные прода после миграции целы (442 пользователя, 173 проекта).

## Запись #41 — FR-08, этап 2: пространства, экран выбора, гейты доступа

**Запрос:** реализовать этап 2 — экран выбора пространства, три состояния плиток, серверные гейты, переключатель в сайдбаре. Наполнения трубы в этом этапе нет, только каркас.

**Реализация:**
- `lib/expert-access.ts` — единая точка правды о доступе: `getSpacesAccess()` считает состояние обеих плиток по роли и когорте, `canEnterExpertSpace()` используется гейтом страниц. Логика не дублируется между экраном выбора, гейтами и API.
- `lib/post-login.ts` — куда вести после входа. Логика была продублирована в трёх местах (`app/page.tsx` дважды, `app/login/page.tsx`), свёл в один хелпер. Админ теперь тоже идёт на экран выбора, а не сразу в админку.
- `app/spaces/page.tsx` + `spaces.module.css` — экран выбора, серверный компонент. Три состояния плитки: открыто, замок (некликабельна), приглашение подключить роль.
- `app/api/expert/access/route.ts` — состояние плиток для переключателя в сайдбаре.
- `app/expert/layout.tsx` — гейт пространства. При закрытом доступе отдаёт заглушку, а не 404 (08.07). Когорту в middleware проверить нельзя (нет доступа к БД), поэтому гейт живёт здесь.
- Заглушки `app/expert/page.tsx`, `join/`, `profile/`, `admin/requests/` — чтобы плитки и гейты вели в существующие адреса. Наполнение — этапы 3–6. `expert/profile` нужен уже сейчас: на него ведёт профильный гейт для роли EXPERT.
- `components/layout/SpaceSwitcher.tsx` + стили — переключатель пространств, вставлен во все три ветки сайдбара. Состояние берёт с сервера.
- `components/layout/AppSidebar.tsx` — сайдбар пространства трубы отдельной ветвью, поставлена до админской: админ внутри трубы видит её навигацию, а не разделы платформы. Экран выбора показывается без сайдбара.
- `middleware.ts` — `/spaces` и `/expert` под защитой; внешний эксперт с платформенных путей уводится на экран выбора; профильный гейт для роли EXPERT ведёт на карточку эксперта; `/expert/profile` и `/api/expert` добавлены в исключения профильного гейта.

**Проверка:** `npx tsc --noEmit` — чисто; `npm run build` — успешно, все шесть новых маршрутов в сборке динамические. Сценарный тест на собранном приложении (не dev), 18 проверок, все зелёные: состояния плиток для всех пяти ролей, заглушка вместо 404 при прямом переходе, блокировка платформы для внешнего эксперта, админский гейт трубы. Регресс существующих страниц — 19 адресов платформы и админки, все 200.

**Локальная среда, две находки:**
- Рабочий стол синхронизируется с iCloud, и macOS выгрузила в облако 22 745 файлов `node_modules` (флаг `dataless`). Чтение упиралось в `ETIMEDOUT`: падали `tsc`, `git`, воркеры `next build`. Лечение: `node_modules` перенесён в `~/.cache/vkr-node-modules`, на его месте симлинк. Сборка ускорилась с 3.1 минуты (с падением) до 3.8 секунды. `package-lock.json` не изменился.
- Локальный `npm start` требует `AUTH_TRUST_HOST=true`, иначе NextAuth v5 отвечает `UntrustedHost`. На проде это настроено, в код ничего не вносилось.

**Файлы:** `lib/expert-access.ts`, `lib/post-login.ts`, `app/spaces/page.tsx`, `app/spaces/spaces.module.css`, `app/api/expert/access/route.ts`, `app/expert/layout.tsx`, `app/expert/page.tsx`, `app/expert/join/page.tsx`, `app/expert/profile/page.tsx`, `app/expert/admin/requests/page.tsx`, `app/expert/expert.module.css`, `components/layout/SpaceSwitcher.tsx`, `components/layout/spaceswitcher.module.css`, `components/layout/AppSidebar.tsx`, `middleware.ts`, `app/page.tsx`, `app/login/page.tsx`.

## Запись #42 — FR-08, этапы 3 и 4: роль эксперта, карточка, админка трубы

**Запрос:** сделать этапы 3 и 4 вместе и протестировать. Резюме в карточке обязательное, фото — нет.

**Реализация, этап 3:**
- `app/api/expert/join/route.ts` — подключение роли эксперта научным руководителем. Роль = наличие `ExpertProfile`, поле `User.role` не трогается. Карточка предзаполняется из профиля НР: место работы, должность, звание, степень, резюме, фото, экспертиза, направления, типы проектов, контакт. Идемпотентно: повторный вызов возвращает `created: false`. Доступно только роли SUPERVISOR.
- `app/api/expert/profile/route.ts` — GET и PUT карточки. Резюме обязательно (400 без него), остальной минимум даёт `cardCompleted`. Для роли EXPERT сохранение карточки ставит `profileCompleted` и cookie — иначе внешний эксперт вечно висел бы на профильном гейте.
- `app/expert/ExpertProfileForm.tsx` + `expert-form.module.css` — форма карточки по образцу профиля НР: теги экспертизы, мультиселекты направлений и типов проектов, загрузка фото и резюме через существующий `/api/upload` либо ссылкой, тумблер скрытия карточки, баннеры «не заполнена» и «скрыта».
- `app/expert/JoinButton.tsx` — кнопка подключения роли, после успеха `updateSession()` и переход на карточку.
- `app/expert/page.tsx` — точка входа: баннеры состояния карточки, ссылка на неё; админа уводит в админку трубы.

**Реализация, этап 4:**
- `app/api/expert/admin/access/route.ts` — GET отдаёт потоки из справочника `cohorts` с флагом открытости и числом студентов, отдельно потоки вне справочника и счётчик студентов без потока; PUT открывает и закрывает поток.
- `app/expert/admin/CohortAccessPanel.tsx` + `access/page.tsx` — галочки по потокам, сводка «открыто потоков, это N студентов», блок про студентов без потока.
- `app/api/admin/invitations/route.ts` — снята бинарность роли: помимо STUDENT и SUPERVISOR принимается EXPERT. Подпись роли в письме и текст про заполнение карточки для эксперта. Логика создания аккаунта, генерации пароля и письма переиспользована целиком.
- `app/expert/admin/ExpertInviteForm.tsx` + `invitations/page.tsx` + `app/api/expert/admin/experts/route.ts` — приглашение внешнего эксперта из админки трубы и список экспертов с состоянием карточки. Контакты экспертов в этом ответе не отдаются.
- `components/layout/AppSidebar.tsx` — в админском сайдбаре трубы три пункта: модерация запросов, доступ по потокам, внешние эксперты.

**Найдено и исправлено при тестировании:** приглашённый админом эксперт, у которого карточки ещё нет, определялся как не-эксперт (проверка шла по наличию записи `ExpertProfile`) и видел на странице трубы текст для студента. Теперь признак — роль EXPERT либо наличие карточки.

**Проверка:** `npx tsc --noEmit` — чисто; `npm run build` — успешно. Три сценарных набора на собранном приложении, 76 проверок, все зелёные:
- 18 — пространства и доступ (этап 2, регресс);
- 35 — подключение роли, идемпотентность, предзаполнение из профиля НР, валидация карточки, отказ роли студенту и админу, доступ по потокам, сквозная проверка «админ закрыл поток → студент потерял доступ → вернул → доступ вернулся», приглашение внешнего эксперта с генерацией пароля;
- 23 — регресс существующих страниц платформы и админки.

Отдельно проверен сквозной путь приглашённого эксперта: письмо с паролем → вход → профильный гейт ведёт на карточку (даже с экрана выбора) → сохранение карточки закрывает гейт → на странице трубы «карточка заполнена и видна студентам».

**Спека:** в разделе 5.2 обязательный минимум дополнен резюме, добавлено пояснение про серверную и клиентскую валидацию; требование 08.08 уточнено.

**Файлы:** `app/api/expert/join/route.ts`, `app/api/expert/profile/route.ts`, `app/api/expert/admin/access/route.ts`, `app/api/expert/admin/experts/route.ts`, `app/api/admin/invitations/route.ts`, `app/expert/ExpertProfileForm.tsx`, `app/expert/JoinButton.tsx`, `app/expert/expert-form.module.css`, `app/expert/expert.module.css`, `app/expert/page.tsx`, `app/expert/join/page.tsx`, `app/expert/profile/page.tsx`, `app/expert/admin/CohortAccessPanel.tsx`, `app/expert/admin/ExpertInviteForm.tsx`, `app/expert/admin/access/page.tsx`, `app/expert/admin/invitations/page.tsx`, `components/layout/AppSidebar.tsx`, `specs/08-FR-08-expert-pipeline.md`.

## Запись #42.1 — Проект вынесен с Рабочего стола из-под iCloud

**Запрос:** сборка и git периодически падали по таймауту; решили перенести проект в несинхронизируемую папку.

**Причина:** Рабочий стол синхронизировался с iCloud Drive, macOS выгружала файлы в облако (флаг `dataless`). Чтение упиралось в `ETIMEDOUT`: `npx tsc` считал 8 минут, `next build` падал обвалом воркеров, `git add` падал с `mmap failed`. Выгружено было 22 745 файлов `node_modules` и 1888 файлов репозитория, в основном объекты `.git`.

**Что сделано:**
- Все выгруженные файлы материализованы перед переносом — иначе за пределами синхронизируемого каталога они стали бы нечитаемыми навсегда.
- Папка «Платформа для НР и студентов» (884 МБ) перенесена из `~/Desktop/mipt работа/` в `~/dev/`. Актуальный путь репозитория: `~/dev/Платформа для НР и студентов/vkr-platform`.
- Промежуточный обходной путь (симлинк `node_modules` в `~/.cache`) больше не нужен: `mv` разрешил ссылку и вернул зависимости внутрь проекта, пустой каталог обхода удалён.

**Проверка после переноса:** `git` работает, история на месте, дерево чистое; Docker подхватил тот же контейнер `vkr-platform-db-1` и тот же том `vkr-platform_pgdata` (имя папки проекта не менялось, поэтому проект compose тот же), данные целы; `prisma generate`, `npx tsc --noEmit` и `npm run build` — чисто, сборка 3.8 секунды; все три сценарных набора прогнаны с нового пути — 18 + 35 + 23 = 76 проверок, все зелёные. Выгруженных файлов в проекте: 0.

**Файлы:** изменений в коде нет, перенос файловый.
